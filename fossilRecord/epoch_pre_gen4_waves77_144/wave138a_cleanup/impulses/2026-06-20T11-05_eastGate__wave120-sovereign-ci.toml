# Wave 120 — Sovereign CI & Final Enrollment
# Issued: Jun 20, 2026 11:05 EDT
# From: eastGate overwatch

[meta]
wave = 120
issued_by = "eastGate_overwatch"
issued_at = "2026-06-20T11:05:00-04:00"
supersedes = "Wave 119 (fossilized)"
entry_state = """
Sovereign CI LIVE on sporeGate. 711 cellMembrane, 963 primalSpring (87 scenarios).
eastGate 13/13, sporeGate 13/13, flockGate 13/13 NUCLEUS.
golgi 18 services (0 failed), depot integrity OK, pepti decommission candidate.
Nest provenance height 3. sporePrint v0.3.0 (175 tests). All VCS parity.
"""
note = """
FOCUSED ATOMIC ROLES:
  flockGate → Tower (BearDog, Songbird, SkunkBat) — WAN periplasm trust
  ironGate  → Node (ToadStool, BarraCuda, CoralReef) — compute trio [BLOCKED: SSH]
  sporeGate → Nest (NestGate, RhizoCrypt, LoamSpine, SweetGrass) + BUILD AUTHORITY
  eastGate  → Meta (BiomeOS, Squirrel, PetalTongue) — orchestration/AI/viz

DECISION: pepti decommission GO (saves $24/mo). golgi stays as sole VPS.
"""

[stream1]
name = "IRONGATE-ENROLLMENT"
owner = "sporeGate_overwatch"
tasks = { irongate_ssh = "P0: RustDesk into 192.168.4.169, add SSH key, deploy NUCLEUS" }

[stream2]
name = "PEPTI-DECOMMISSION"
owner = "sporeGate_overwatch"
tasks = { decision = "✅ GO — operator approved Jun 20", execute = "Stop services, remove WG peer from golgi, destroy droplet, update manifests. golgi stays as sole VPS." }

[stream3]
name = "HARDWARE"
owner = "operator"
tasks = { flint2_install = "Physical install this weekend — replaces Eero at Hub 2" }

[stream4]
name = "TOWER-WORK"
owner = "flockGate_team"
tasks = { beardog_btsp = "BTSP trust bootstrap over WAN mesh", songbird_mesh = "mesh.init topology-aware routing over WG", skunkbat_threat = "Threat detection + defense attestation" }

[stream5]
name = "META-WORK"
owner = "eastGate_team"
tasks = { primalspring_expand = "87 scenarios → expand coverage (963 lib tests)", squirrel_pipeline = "AI pipeline + provenance proxy wired", petaltongue_viz = "Visualization + dashboard" }

[stream6]
name = "NEST-WORK"
owner = "sporeGate_overwatch"
tasks = { ledger_commits = "Periodic provenance commits on each wave", mesh_init = "P2: topology-aware routing setup", omada_config = "P2: VLAN config at 192.168.4.111 (admin/admin)", relay_push = "P2: strandGate + southGate sovereign config via RustDesk" }
