[impulse]
id = "2026-06-02T07-44-eastGate-wave68-biomegate-exp234-hpc-update"
type = "frago"
priority = "routine"
wave = 68

[from]
gate = "eastGate"

[to]
gates = ["biomeGate"]

[content]
subject = "Wave 68 — Exp 234 Run #6 + PBDMA root cause identified, sovereign compute bifurcated"
body = """
Follow-up to 2026-06-01T13-32 FRAGO (wave67-biomegate-airgap-exp234-cleared).

HPC STATUS UPDATE:

WORKING PATH — wgpu/DRM on RTX 5060 Blackwell:
- toadStool wgpu_dispatch.rs backend: end-to-end verified 256/256 f32 correct
- SPIR-V validation + naga/WGSL fallback when coralReef returns internal format
- device-lost guard, panic-abort resilience, dedicated thread for wgpu init
- ipc.watch integration for live shader provider re-discovery (GAP-HS-119 resolved)
This is the PRODUCTION path for sovereign compute today.

BLOCKED PATH — VFIO bare-metal on Titan V:
- ROOT CAUSE IDENTIFIED: PBDMA runlist never configured
  - PFIFO_RUNLIST_BASE = 0 (no runlist)
  - PBDMA_CHANNEL(0) = 0 (PBDMA not bound to channel)
  - 7 pushbuffer entries submitted; GP_GET never advances
- FIX: build runlist in DMA-mapped memory, write RUNLIST_BASE (0x002270),
  trigger RUNLIST_SUBMIT (0x002274), optionally poke FECS mailbox for GR context
- This is the path to full vendor-atheistic sovereignty

REQUESTED ACTIONS:

1. EXP 234 RUN #6 — reboot to clear stuck nvsov from Run #5, then execute
   sovereign.warm_handoff with nvidia_catalyst_minimal_nop_titanv.
   On success: channel-adoption -> shader-dispatch -> Tier 3.

2. PBDMA RUNLIST — implement Host engine registration in toadStool cylinder.
   See HOTSPRING_TIER2_PBDMA_ROOT_CAUSE_JUN01_2026.md in wateringHole handoffs.

3. FECS CONTEXT — address PENDING_CTX_RELOAD after runlist binding.
   FECS is alive but os=0 (no context loaded).

4. ACK — confirm receipt and execution plan. Independent of mesh cutover.

Ecosystem deep debt complete. Cascade 38/38 parity.
Pull wateringHole for all updates including latest handoffs.
"""

[meta]
created = "2026-06-02T07:44:00-04:00"
ack_required = true
references = ["2026-06-01T13-32-eastGate-wave67-biomegate-airgap-exp234-cleared"]
