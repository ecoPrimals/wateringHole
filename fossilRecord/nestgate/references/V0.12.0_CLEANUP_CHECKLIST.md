# v0.12.0 Cleanup Checklist - Deprecation Removal
**Target Release**: v0.12.0  
**Scheduled Date**: May 2026  
**Purpose**: Remove deprecated modules after 6-month migration period

---

## 📊 OVERVIEW

### What's Being Removed
- **6 deprecated modules** (1,003 lines of code estimated)
- **92+ deprecation markers** across 59+ files
- **Legacy re-exports** for backward compatibility
- **Zero-cost security provider modules** (deprecated v0.9.0)
- **NEW (November 10, 2025)**: 2 duplicate canonical provider traits
- **NEW (November 10, 2025)**: 17 redundant Result type aliases
- **NEW (November 10, 2025)**: 13 duplicate security trait definitions
- **NEW (November 10, 2025)**: 1 deprecated stub module location

### Why Removal is Safe
- ✅ 6-month grace period (Nov 2025 → May 2026)
- ✅ Clear migration paths documented
- ✅ Canonical replacements established
- ✅ Deprecation warnings guide users
- ✅ All internal code already migrated

---

## 🎯 MODULES TO REMOVE

### 1. unified_config_consolidation.rs (490 lines)

**Location**: `code/crates/nestgate-core/src/unified_config_consolidation.rs`

**Status**: ✅ Fully deprecated since Nov 7, 2025

**Replacement**: `config::canonical_primary`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::unified_config_consolidation::StandardDomainConfig;

// NEW (canonical):
use nestgate_core::config::canonical_primary::domains::*;
```

**Checklist**:
- [ ] Verify no external crates depend on this module
- [ ] Check workspace for any remaining imports
- [ ] Remove file: `unified_config_consolidation.rs`
- [ ] Update CHANGELOG
- [ ] Run full test suite

**Command**:
```bash
# Check for external dependencies
cargo tree --workspace | grep unified_config_consolidation

# Check for internal usage
grep -r "unified_config_consolidation" code/crates/ --exclude-dir=target

# Remove if clean
rm code/crates/nestgate-core/src/unified_config_consolidation.rs

# Update mod.rs
# Remove: pub mod unified_config_consolidation;
```

---

### 2. traits_root/ (95 lines)

**Location**: `code/crates/nestgate-core/src/traits_root/`

**Status**: ✅ Re-exports only, fully deprecated

**Replacement**: `traits::canonical_unified_traits`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::traits_root::CanonicalService;

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalService;
```

**Checklist**:
- [ ] Verify all 75 import sites migrated
- [ ] Check for external crate dependencies
- [ ] Remove directory: `traits_root/`
- [ ] Update parent mod.rs
- [ ] Run trait-specific tests

**Command**:
```bash
# Check usage
grep -r "traits_root" code/crates/ --exclude-dir=target

# Check external deps
cargo tree --workspace | grep traits_root

# Remove if clean
rm -rf code/crates/nestgate-core/src/traits_root/
```

---

### 3. error/idiomatic/ (63 lines)

**Location**: `code/crates/nestgate-core/src/error/idiomatic/`

**Status**: ✅ Fully replaced by unified_result_system

**Replacement**: `error::unified_result_system`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::error::idiomatic::IdioResult;
type MyResult<T> = IdioResult<T>;

// NEW (canonical):
use nestgate_core::Result;
// Just use Result<T> directly
```

**Checklist**:
- [ ] Verify 3 remaining usages migrated
- [ ] Check macro usage in codebase
- [ ] Remove directory: `error/idiomatic/`
- [ ] Update error/mod.rs
- [ ] Run error handling tests

**Command**:
```bash
# Check active usage (not comments)
grep -r "^[^/]*use.*error::idiomatic" code/crates/ --exclude-dir=target

# Check macro usage
grep -r "network_error!\|storage_error!\|validation_error!" code/crates/

# Remove if clean
rm -rf code/crates/nestgate-core/src/error/idiomatic/
```

---

### 4. zero_cost_security_provider/ (~355 lines)

**Location**: `code/crates/nestgate-core/src/zero_cost_security_provider/`

**Status**: ✅ Fully deprecated since v0.9.0

**Replacement**: `traits::canonical_unified_traits::CanonicalSecurity`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::zero_cost_security_provider::{
    ZeroCostSecurityProvider, 
    AuthenticationProvider,
    EncryptionProvider
};

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalSecurity;
// CanonicalSecurity includes all security operations with zero-cost patterns integrated
```

**Checklist**:
- [ ] Verify all usages migrated to CanonicalSecurity
- [ ] Check for any benchmarks still using old traits
- [ ] Remove directory: `zero_cost_security_provider/`
- [ ] Update lib.rs/mod.rs
- [ ] Run security-related tests

**Command**:
```bash
# Check active usage
grep -r "zero_cost_security_provider" code/crates/ --exclude-dir=target

# Check for trait implementations
grep -r "impl.*ZeroCostSecurityProvider" code/crates/ --exclude-dir=target

# Remove if clean
rm -rf code/crates/nestgate-core/src/zero_cost_security_provider/
```

---

### 5. universal_providers_zero_cost.rs (~200 lines)

**Location**: `code/crates/nestgate-core/src/universal_providers_zero_cost.rs`

**Status**: ✅ Deprecated since v0.9.0 (partial module)

**Replacement**: `traits::canonical_unified_traits` and `traits::canonical_provider_unification`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::universal_providers_zero_cost::{
    ZeroCostSecurityProvider,
    ZeroCostUniversalSecurityWrapper
};

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalSecurity;
use nestgate_core::traits::canonical_provider_unification::SecurityProvider;
```

**Checklist**:
- [ ] Verify deprecated trait usages migrated
- [ ] Check if any non-deprecated code remains
- [ ] Either remove file or clean up deprecated sections
- [ ] Update lib.rs/mod.rs
- [ ] Run provider-related tests

**Command**:
```bash
# Check usage of deprecated traits from this file
grep -r "universal_providers_zero_cost::ZeroCostSecurityProvider" code/crates/ --exclude-dir=target

# Review file for non-deprecated content
cat code/crates/nestgate-core/src/universal_providers_zero_cost.rs | grep -v "deprecated"

# Remove or update as needed
```

---

### 6. zero_cost/traits.rs (partial - security section)

**Location**: `code/crates/nestgate-core/src/zero_cost/traits.rs` (lines 16-30)

**Status**: ✅ ZeroCostSecurityProvider deprecated since v0.9.0

**Replacement**: `traits::canonical_unified_traits::CanonicalSecurity`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::zero_cost::traits::ZeroCostSecurityProvider;

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalSecurity;
```

**Checklist**:
- [ ] Remove deprecated ZeroCostSecurityProvider trait (lines 16-30)
- [ ] Keep other non-deprecated zero-cost traits (cache, etc.)
- [ ] Update module documentation
- [ ] Verify tests still pass

**Command**:
```bash
# Check usage
grep -r "zero_cost::traits::ZeroCostSecurityProvider" code/crates/ --exclude-dir=target

# Remove specific trait definition (manual edit or script)
# Keep ZeroCostCacheProvider and other legitimate traits
```

---

### 7. Duplicate Canonical Provider Traits (November 10, 2025)

**Locations**: 
- `code/crates/nestgate-core/src/traits/canonical_hierarchy.rs` (`CanonicalProvider<T>`)
- `code/crates/nestgate-core/src/traits/canonical_provider_unification.rs` (`CanonicalUniversalProvider<T>`)

**Status**: ✅ Deprecated v0.11.2 (November 10, 2025)

**Replacement**: `traits::canonical_unified_traits::CanonicalProvider<T>`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::traits::canonical_hierarchy::CanonicalProvider;
// or
use nestgate_core::traits::canonical_provider_unification::CanonicalUniversalProvider;

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalProvider;
```

**Checklist**:
- [ ] Verify no implementations of deprecated traits remain
- [ ] Remove deprecated trait definitions
- [ ] Keep migration guide: `CANONICAL_PROVIDER_COMPARISON.md`
- [ ] Run provider-related tests

**Command**:
```bash
# Check usage
grep -r "canonical_hierarchy::CanonicalProvider\|canonical_provider_unification::CanonicalUniversalProvider" code/crates/ --exclude-dir=target

# Remove deprecations (or keep file with deprecation marker removed)
```

---

### 8. Redundant Result Type Aliases (November 10, 2025)

**Location**: `code/crates/nestgate-core/src/result_types.rs` (lines 269-387)

**Status**: ✅ 17 aliases deprecated v0.11.2 (November 2025)

**Replacement**: `Result<T>` or `CanonicalResult<T>`

**Aliases to Remove**:
1. `ValidationResult<T>`
2. `StorageResult<T>`
3. `SecurityResult<T>`
4. `ApiResult<T>`
5. `McpResult<T>`
6. `PerformanceResult<T>`
7. `HandlerResult<T>`
8. `SerializationResult<T>`
9. `DatabaseResult<T>`
10. `CacheResult<T>`
11. `WorkflowResult<T>`
12. `MonitoringResult<T>`
13. `ConfigResult<T>`
14. `TestingResult<T>`
15. `ZfsResult<T>`
16. (2 more in other locations)

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::result_types::ApiResult;
pub fn handler() -> ApiResult<Data> { ... }

// NEW (canonical):
use nestgate_core::Result;
pub fn handler() -> Result<Data> { ... }
```

**Checklist**:
- [ ] Remove deprecated type aliases (lines 269-387)
- [ ] Keep canonical types (Result, CanonicalResult, VoidResult, TestResult)
- [ ] Keep function types (ConnectionFactory, HealthCheckFn, ValidatorFn)
- [ ] Verify no usage of deprecated aliases
- [ ] Run full test suite

**Command**:
```bash
# Check usage of deprecated aliases
grep -r "ApiResult\|StorageResult\|SecurityResult" code/crates/ --exclude-dir=target

# Edit result_types.rs to remove deprecated section
```

---

### 9. Duplicate Security Trait Definitions (November 10, 2025)

**Locations**: 13 deprecated security traits across multiple files

**Status**: ✅ Most deprecated v0.9.0-v0.11.3

**Replacement**: `traits::canonical_unified_traits::CanonicalSecurity`

**Traits to Remove**:
1. `canonical_hierarchy.rs::CanonicalSecurity` (duplicate)
2. `canonical_provider_unification.rs::SecurityProvider` (duplicate)
3. `canonical_provider_unification.rs::SecurityService` (marker)
4. `zero_cost_security_provider/traits.rs::ZeroCostSecurityProvider` (old)
5. `zero_cost_security_provider/traits.rs::AuthenticationProvider` (sub-trait)
6. `zero_cost_security_provider/traits.rs::SecurityHealthProvider` (sub-trait)
7. `zero_cost_security_provider/traits.rs::SecurityMetricsProvider` (sub-trait)
8. `universal_traits/security.rs::SecurityPrimalProvider` (old)
9. `universal_providers_zero_cost.rs::ZeroCostSecurityProvider` (duplicate)
10. `traits/native_async.rs::NativeAsyncSecurityProvider` (old)
11. `services/native_async/traits.rs::NativeAsyncSecurityService` (service)
12. `universal_providers.rs::SecurityClient` (client)
13. `zero_cost/native_async_traits.rs::NativeAsyncSecurityProvider` (duplicate)
14. `zero_cost/traits.rs::ZeroCostSecurityProvider` (duplicate)

**Migration Path**:
```rust
// OLD (any of the above):
use nestgate_core::zero_cost_security_provider::ZeroCostSecurityProvider;

// NEW (canonical):
use nestgate_core::traits::canonical_unified_traits::CanonicalSecurity;
```

**Checklist**:
- [ ] Remove all 13 deprecated security trait definitions
- [ ] Verify no implementations remain
- [ ] Keep canonical: `CanonicalSecurity`
- [ ] Keep migration doc: `SECURITY_TRAIT_CONSOLIDATION_COMPLETE.md`
- [ ] Run security-related tests

**Command**:
```bash
# Check for implementations of old traits
grep -r "impl.*ZeroCostSecurityProvider\|impl.*SecurityPrimalProvider" code/crates/ --exclude-dir=target

# Remove deprecated definitions
```

---

### 10. Deprecated Stub Module Location (November 10, 2025)

**Location**: `code/crates/nestgate-core/src/universal_primal_discovery/stubs.rs`

**Status**: ✅ Deprecated v0.11.2 (November 10, 2025), now re-exports from `dev_stubs`

**Replacement**: `dev_stubs::primal_discovery`

**Migration Path**:
```rust
// OLD (deprecated):
use nestgate_core::universal_primal_discovery::stubs::*;

// NEW (organized):
use nestgate_core::dev_stubs::primal_discovery::*;
// or
use nestgate_core::dev_stubs::*;
```

**Checklist**:
- [ ] Remove old `universal_primal_discovery/stubs.rs` file
- [ ] Keep `dev_stubs/primal_discovery.rs` (new location)
- [ ] Verify no usage of old import path
- [ ] Run stub-related tests

**Command**:
```bash
# Check usage of old path
grep -r "universal_primal_discovery::stubs" code/crates/ --exclude-dir=target

# Remove old file
rm code/crates/nestgate-core/src/universal_primal_discovery/stubs.rs
```

---

## 📋 DETAILED CHECKLIST

### Phase 1: Pre-Removal Verification (Week 1)

#### A. Dependency Check
- [ ] Run `cargo tree --workspace` to check external dependencies
- [ ] Search for deprecated module imports across workspace
- [ ] Check if any examples use deprecated modules
- [ ] Verify all documentation updated
- [ ] Check if any scripts reference deprecated paths

#### B. Migration Verification
- [ ] Verify all 92 deprecation sites migrated
- [ ] Check all import paths updated
- [ ] Run full test suite (should be clean)
- [ ] Verify no production code uses deprecated modules
- [ ] Check integration tests

#### C. Documentation Review
- [ ] Review migration guides are complete
- [ ] Update CHANGELOG with removal notice
- [ ] Update API documentation
- [ ] Check README and quick start guides
- [ ] Verify examples are up to date

---

### Phase 2: Removal Execution (Week 2)

#### A. Create Feature Branch
```bash
git checkout -b feature/v0.12.0-deprecation-removal
```

#### B. Remove Modules (In Order)
1. **Remove unified_config_consolidation.rs**
   ```bash
   rm code/crates/nestgate-core/src/unified_config_consolidation.rs
   # Remove from lib.rs or mod.rs
   ```

2. **Remove traits_root/**
   ```bash
   rm -rf code/crates/nestgate-core/src/traits_root/
   # Remove from lib.rs or mod.rs
   ```

3. **Remove error/idiomatic/**
   ```bash
   rm -rf code/crates/nestgate-core/src/error/idiomatic/
   # Remove from error/mod.rs
   ```

#### C. Update Module Declarations
- [ ] Update `code/crates/nestgate-core/src/lib.rs`
- [ ] Update `code/crates/nestgate-core/src/error/mod.rs`
- [ ] Remove re-export statements
- [ ] Clean up any related imports

---

### Phase 3: Verification (Week 2)

#### A. Build Verification
```bash
# Clean build
cargo clean
cargo check --workspace

# Should compile with 0 errors and 0 warnings
# (no more deprecation warnings!)
```

- [ ] `cargo check --workspace` - 0 errors, 0 warnings
- [ ] `cargo build --workspace --release` - success
- [ ] No deprecation warnings remaining

#### B. Test Verification
```bash
# Run full test suite
cargo test --workspace

# Should maintain 100% pass rate
```

- [ ] All unit tests passing
- [ ] All integration tests passing
- [ ] All doc tests passing
- [ ] No test regressions

#### C. Clippy & Fmt
```bash
cargo clippy --workspace -- -D warnings
cargo fmt --all -- --check
```

- [ ] Clippy clean (no warnings)
- [ ] Formatting consistent
- [ ] No new lints introduced

---

### Phase 4: Documentation Update (Week 2)

#### A. Update Changelogs
- [ ] Add to CHANGELOG.md:
  ```markdown
  ## [0.12.0] - 2026-05-XX
  
  ### Removed (Breaking Change)
  - **BREAKING**: Removed deprecated `unified_config_consolidation` module
    - Migration: Use `config::canonical_primary` instead
  - **BREAKING**: Removed deprecated `traits_root` module  
    - Migration: Use `traits::canonical_unified_traits` instead
  - **BREAKING**: Removed deprecated `error::idiomatic` module
    - Migration: Use `error::unified_result_system` instead
  
  ### Migration Guide
  See MIGRATION_V0.12.0.md for detailed migration instructions.
  ```

#### B. Update Documentation
- [ ] Update ARCHITECTURE_OVERVIEW.md (remove deprecated sections)
- [ ] Update UNIFICATION_MASTER.md (mark 100% complete)
- [ ] Update PROJECT_STATUS_MASTER.md
- [ ] Update API docs
- [ ] Update quick start guides

#### C. Create Migration Guide
- [ ] Create `MIGRATION_V0.12.0.md`
- [ ] Document all breaking changes
- [ ] Provide before/after examples
- [ ] List deprecated → canonical mappings

---

### Phase 5: Release Preparation (Week 3)

#### A. Version Bump
- [ ] Update version in Cargo.toml: `0.11.x` → `0.12.0`
- [ ] Update dependencies if needed
- [ ] Run `cargo update`
- [ ] Verify lockfile

#### B. Release Notes
- [ ] Create comprehensive release notes
- [ ] Highlight breaking changes prominently
- [ ] Provide migration examples
- [ ] Link to migration guide

#### C. Testing on Clean Environment
- [ ] Test fresh clone
- [ ] Test on different OS (Linux, macOS, Windows if applicable)
- [ ] Verify examples work
- [ ] Check documentation builds

---

### Phase 6: Release (Week 3)

#### A. Final Verification
- [ ] All checklist items complete
- [ ] All tests passing
- [ ] Documentation updated
- [ ] CHANGELOG complete
- [ ] Migration guide ready

#### B. Git Operations
```bash
# Merge feature branch
git checkout main
git merge feature/v0.12.0-deprecation-removal

# Tag release
git tag -a v0.12.0 -m "v0.12.0 - Deprecation removal"
git push origin main --tags
```

#### C. Publish (if applicable)
- [ ] Publish to crates.io (if public)
- [ ] Update documentation site
- [ ] Announce breaking changes
- [ ] Provide support for migration questions

---

## 🔍 VERIFICATION COMMANDS

### Quick Verification Script
```bash
#!/bin/bash
# verify_removal.sh

echo "Checking for deprecated module references..."

# Check unified_config_consolidation
echo "1. unified_config_consolidation:"
grep -r "unified_config_consolidation" code/crates/ --exclude-dir=target | wc -l

# Check traits_root
echo "2. traits_root:"
grep -r "traits_root" code/crates/ --exclude-dir=target | wc -l

# Check error::idiomatic
echo "3. error::idiomatic:"
grep -r "error::idiomatic" code/crates/ --exclude-dir=target | wc -l

echo ""
echo "All counts should be 0 for clean removal."
echo ""

# Build verification
echo "Running build verification..."
cargo check --workspace 2>&1 | grep -E "error|warning" | head -10

# Test verification
echo ""
echo "Running test verification..."
cargo test --workspace --lib 2>&1 | grep "test result"
```

### Expected Output (After Removal)
```
Checking for deprecated module references...
1. unified_config_consolidation: 0
2. traits_root: 0
3. error::idiomatic: 0

All counts should be 0 for clean removal.

Running build verification...
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 7.52s
(no errors or warnings)

Running test verification...
test result: ok. 1917 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

---

## 📊 METRICS TO TRACK

### Before Removal
- Total lines of code: X
- Deprecated modules: 3 (648 lines)
- Deprecation warnings: 92
- Build time: Y seconds
- Test count: 1,917

### After Removal (Expected)
- Total lines of code: X - 648
- Deprecated modules: 0
- Deprecation warnings: 0
- Build time: Y - δ seconds (slightly faster)
- Test count: 1,917 (maintained)

---

## ⚠️ RISK ASSESSMENT

### Low Risk
- ✅ 6-month deprecation period
- ✅ Clear migration paths
- ✅ All internal code migrated
- ✅ Deprecation warnings guide users
- ✅ Comprehensive documentation

### Mitigation Strategies
1. **External Dependencies**: Check cargo tree before removal
2. **User Impact**: Announce in advance, provide migration guide
3. **Rollback Plan**: Keep feature branch for quick revert if needed
4. **Support**: Monitor issues/questions, provide migration help

---

## 📅 TIMELINE

### Months Before (Nov 2025 - Apr 2026)
- ✅ Deprecation warnings active (done)
- ✅ Migration guides available (done)
- ✅ Users have time to migrate

### Release Month (May 2026)
- **Week 1**: Pre-removal verification
- **Week 2**: Removal execution + verification
- **Week 3**: Release preparation + launch
- **Week 4+**: Support and migration assistance

---

## 🎯 SUCCESS CRITERIA

### Removal is Successful When:
- [ ] All 3 modules removed
- [ ] Zero compilation errors
- [ ] Zero deprecation warnings
- [ ] All 1,917+ tests passing
- [ ] Documentation updated
- [ ] CHANGELOG complete
- [ ] Migration guide published
- [ ] Clean builds on all platforms

### Quality Gates:
- [ ] `cargo check --workspace` - clean
- [ ] `cargo test --workspace` - 100% pass
- [ ] `cargo clippy --workspace` - no warnings
- [ ] Documentation builds successfully
- [ ] Examples run correctly

---

## 📞 SUPPORT PLAN

### User Migration Support
- Create GitHub discussion for migration questions
- Monitor issues for deprecation-related problems
- Provide example PRs showing migration
- Update documentation based on common questions

### Internal Team
- Brief team on breaking changes
- Review migration guide
- Test on representative projects
- Prepare for user questions

---

## 🎉 POST-REMOVAL BENEFITS

### Code Quality
- ✅ 648 fewer lines of deprecated code
- ✅ Zero deprecation warnings
- ✅ 100% unification achieved
- ✅ Cleaner architecture

### Developer Experience
- ✅ No confusion about which module to use
- ✅ Simpler API surface
- ✅ Faster compilation (less code)
- ✅ Easier maintenance

### Project Health
- ✅ Technical debt eliminated
- ✅ Clean codebase
- ✅ Production-ready
- ✅ Sustainable architecture

---

## 📚 REFERENCES

- `UNIFICATION_MASTER.md` - Current unification status
- `CONFIG_UNIFICATION_100_PERCENT_COMPLETE.md` - Config migration
- `ERROR_SYSTEM_UNIFICATION_COMPLETE.md` - Error system migration
- `PHASE_4_COMPLETE_NOV_7.md` - Traits migration
- `UNIFICATION_OPPORTUNITIES_REPORT_NOV_8_2025.md` - Current analysis

---

**Checklist Version**: 2.0  
**Created**: November 8, 2025  
**Updated**: November 10, 2025 (added 4 new deprecation categories from Week 2 work)  
**Target Release**: v0.12.0 (May 2026)  
**Status**: Ready for execution in May 2026

---

## 📝 UPDATE LOG

### Version 2.0 - November 10, 2025
**Added**:
- Section 7: Duplicate Canonical Provider Traits (2 traits)
- Section 8: Redundant Result Type Aliases (17 aliases)
- Section 9: Duplicate Security Trait Definitions (13 traits)
- Section 10: Deprecated Stub Module Location (1 file)

**Summary**: Added 33 new deprecation items from Week 2 analysis and consolidation work.

### Version 1.0 - November 8, 2025
**Initial**: 6 deprecated modules from earlier work

---

*This checklist will be updated as we approach the May 2026 release date.*

