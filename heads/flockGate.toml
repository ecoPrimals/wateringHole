# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: flockGate | Updated: 2026-07-18T20:33:00Z
#
# This file is owned exclusively by flockGate. No other gate writes here.

[meta]
gate = "flockGate"
updated = "2026-07-18T20:33:00Z"
wave = "150g"

[heads]
primalSpring = "c677c3c"
wateringHole = "a70076f3"
esotericWebb = "08588d5"
plasmidBin = "e0b20e0"

[mesh]
node_id = "flockGate"
songbird_version = "0.2.1"
wireguard_status = "UP"
wireguard_addr = "10.13.37.6/24"
peers_mesh_registered = 2
federation_sporegate_7700 = "LIVE"
federation_golgi_7700 = "LIVE"
status = "6-GATE MESH OPERATIONAL — ZERO P1"

[esotericwebb]
status = "LIVE — V19.1 HTTP+JSON-RPC, systemd + linger"
version = "V19.1"
commit = "08588d5"
systemd = "esotericwebb-server.service ENABLED"
linger = "YES — survives logout"
protocol = "HTTP POST /jsonrpc + raw TCP JSON-RPC (dual)"
capabilities = 26
primals_connected = 6
webb_domain = "webb.primals.eco"
wan_status = "200 OK — 228ms WAN RTT"

[wan_subdomain_posture]
footprint_primals_eco = "200 — 165ms"
webb_primals_eco = "200 — 228ms (POST /jsonrpc)"
sporeprint_primals_eco = "200 — 180ms"
live_primals_eco = "200 — 311ms"
git_primals_eco = "200 — 162ms"
primals_eco_root = "301 → sporeprint.primals.eco"
all_surfaces_live = true
p1_remaining = 0

[deployment_chain]
architecture = "Cloudflare → Caddy (golgi) → WireGuard → service"
subdomain_standard = "prefix.primals.eco (REQUIRED)"
three_domains = ["primals.eco (intra-membrane)", "primal.eco (inner membrane)", "nestgate.io (data service)"]

[primalspring]
version = "v0.9.39"
head = "c677c3c"
scenarios = 169
tests = 1203
failures = 0
suite_status = "GREEN"

[remaining_divergence]
p0_items = 0
p1_items = 0
p2_items = ["esotericWebb GET handler", "V19.1 depot binary push", "primalSpring CAC scenario", "primals.eco DNSSEC", "footprint_composition.toml URL update"]
