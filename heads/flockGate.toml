# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: flockGate | Updated: 2026-07-23T20:48:00Z
#
# This file is owned exclusively by flockGate. No other gate writes here.

[meta]
gate = "flockGate"
updated = "2026-07-23T20:48:00Z"
wave = "150w"

[heads]
primalSpring = "41cb0a0"
wateringHole = "ca95a3d8"
esotericWebb = "2e5c2e5"
songBird = "cfa84a2"
petalTongue = "c6baa1d"

[remotes]
standard = "Forgejo-first (origin=Forgejo, github=mirror)"
swap_completed = true

[mesh]
node_id = "flockGate"
songbird_version = "0.2.1"
wireguard_status = "UP"
wireguard_addr = "10.13.37.6/24"
songbird_peers_reachable = 4
songbird_mesh_status = "initialized, 4 direct paths, relay enabled"
status = "5-GATE ACTIVE MESH + SONGBIRD 4-PEER OPERATIONAL"

[tower_shadow]
status = "ACTIVE"
interval = "60min"
results = 16
latest = "wireguard_southGate_20260723_163648.json"
peers_benchmarked = ["golgi", "ironGate", "northGate", "southGate", "sporeGate"]

[songbird]
status = "LIVE — P0 fixes deployed, mesh 4/4 reachable"
version = "0.2.1"
commit = "cfa84a2"
uds = "/run/user/1000/biomeos/songbird.sock"
http_port = 7780
federation_port = 7700
peers_online = 4
capability_routing = "PROVEN LIVE (Domain 1 confirmed)"
fixes_deployed = ["mesh.prune_stale", "socket dir guard", "drawbridge diagnostics"]

[esotericwebb]
status = "LIVE — V22, scene graph pipeline ACTIVE via petalTongue v1.7"
version = "V22"
commit = "2e5c2e5"
systemd = "esotericwebb-server.service ENABLED + LINGER"
uptime = "3 days (stable since 150o fix)"
primals_connected = 6
webb_domain = "webb.primals.eco"
wan_post = "200 OK (196ms)"
wan_get = "200 OK (196ms)"

[petaltongue]
status = "DEPLOYED — v1.7.0 IPC server active"
version = "1.7.0"
commit = "c6baa1d"
systemd = "petaltongue-server.service ENABLED"
uptime = "1.5 days"
uds = "/run/user/1000/biomeos/petaltongue.sock"

[wan_subdomain_posture]
webb_primals_eco = "200 (196ms)"
footprint_primals_eco = "200 (203ms)"
sporeprint_primals_eco = "200 (179ms)"
git_primals_eco = "200 (160ms)"
all_surfaces_live = true

[primalspring]
head = "41cb0a0"
scenarios = 182
tests = 1225
failures = 0
suite_status = "GREEN"
new_scenarios = ["s_tower_capability_routing", "s_tower_multi_stack", "s_tower_large_data", "s_tower_secure_compute", "s_tower_compute_mesh", "s_tower_edge_profile"]
known_debt = ["graphenegate-readiness:14", "composition-access-control:15", "arch-fitness:1"]

[remaining_divergence]
p0_items = 0
p0_resolved = ["tower.shadow enabled (already active)", "songBird mesh fixed (4/4 reachable)"]
p1_items = ["checksums.toml format migration (eastGate code task)", "drawbridge JSON-RPC→HTTP translation (eastGate)"]
p2_items = ["Android Keystore + grapheneGate test", "footPrint declarative source registry"]
