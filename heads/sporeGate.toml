# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-27T13:45:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-29T10:45:00Z"
wave = "155h"

[heads]
bearDog = "a8bba76"
songBird = "c4c5d2d"
nestGate = "263e68a"
biomeOS = "4667f58"
rhizoCrypt = "d4972b0"
squirrel = "a9493a8"
sweetGrass = "076337c"
loamSpine = "e1a8749"
skunkBat = "a872c84"
coralReef = "94ee28a"
barraCuda = "ceca2b1"
petalTongue = "f61c808"
toadStool = "40d0ae3"
sourDough = "3a0b52d"
cellMembrane = "473fd7e"
primalSpring = "05e72e4"
wateringHole = "b8ca9f7"
sporePrint = "67cc325"

[deploy]
songbird_version = "0.2.1 (c4c5d2d) — GateEnroll dispatch + drawbridge body-method fallback"
beardog_version = "0.9.0 (a8bba76) — FIDO2 + iosGate + HSM agnostic (Wave 152-154)"
rhizocrypt_fix = "d4972b0 — Windows cross-compile (cfg(unix) gate on publish_capability_manifest)"
petaltongue_version = "1.7.0"
membrane_version = "0.1.0 (473fd7e)"
gateway_status = "ENROLLMENT_ENDPOINT_LIVE"
depot_status = "FRESH — 13 primals × 3 targets (x86_64-musl, aarch64-musl, x86_64-windows-gnu), provenance tracked"
mesh_status = "8-GATE MESH (5 enrolling)"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active"

[tower]
status = "TOWER ATOMIC COMPLETE — 3/3 LIVE, ENROLLMENT ENDPOINT DEPLOYED"
songbird = "LIVE — GateEnroll wired, drawbridge /enroll active, Caddy TLS proxy"
beardog = "LIVE — AUTH_MODE=enforced, FIDO2+iosGate shipped (Wave 154)"
skunkbat = "LIVE (PUBLIC)"

[enrollment_endpoint]
status = "PHASE 0 COMPLETE — LIVE ON GOLGI"
drawbridge_addr = "127.0.0.1:7780"
caddy_route = "primals.eco/enroll → reverse_proxy 127.0.0.1:7780"
method = "mesh.gate_enroll"
local_test = "PASS — param validation (missing field gate_name)"
wan_test = "PASS — HTTPS via Caddy (param validation)"
token_test = "PASS — progresses to wg_public_key validation"
legacy_gateway = "DISABLED — songbird-gateway.service replaced by songbird-membrane.service"
forgejo_api_token = "gate-enrollment-service (write:admin,write:user,write:organization)"

[mesh_enrollment]
wave = "155a"
golgi_peers = 8
registered = ["southGate (10.13.37.9)", "strandGate (10.13.37.10)", "westGate (10.13.37.11)"]
pending_keygen = ["blueGate (10.13.37.12)", "swiftGate (10.13.37.13)"]
forgejo_ssh = "strandGate key registered (ID 12)"
dns_advisory = "CRITICAL: enrollment template DNS=10.13.37.1 causes 3-5s lag — REMOVE"

[tower_shadow]
status = "ACTIVE — hourly data"
interval = "60min"
gates_active = ["sporeGate", "flockGate", "golgiBody"]

[benchmark]
phase = "PHASE 1 PASS — Tower EXCEEDS WireGuard"
lan_tower = "0.454ms avg (353x vs WG overlay)"
wan_tower = "76.8ms avg, 1.7x sustained"
iperf3_lan = "2.36 Gbps sporeGate→eastGate (CONFIRMED)"

[harvest]
wave = "155b"
harvested = "2026-07-27T16:00:00Z"
x86_64_musl = 13
aarch64_musl = 13
x86_64_windows = 13
provenance = "builder=sporeGate, toolchain=rustc 1.96.1"
golgi_pushed = "CONFIRMED — 3 targets synced (x86_64-musl, aarch64-musl, x86_64-windows-gnu)"
note = "Full fleet depot — all compositions + Windows gates covered"

[hardening]
beardog_auth_mode = "enforced (was Permissive)"
beardog_uds_btsp = "required (was OFF)"
method = "systemd drop-in override"
deployed = "2026-07-26T01:35:17Z"

[credentials]
gsc_service_account = "DEPLOYED to golgi:/opt/ecoPrimals/credentials/gsc-service-account.json (chmod 600)"

[primalspring]
head = "05e72e4"
scenarios = 197
known_debt = "3 (grapheneGate provenance 2, sporePrint composition 1)"
suite_status = "GREEN — 1241 passed, 0 failed"

[surfaces]
sporeprint = "200"
footprint = "200"
live = "200"
webb = "200"
git = "200"
root = "301"
enroll = "200 (JSON-RPC param validation)"

[resolved]
tower_atomic = "COMPLETE — 7/7 debt resolved"
depot_divergence = "RESOLVED — 28 binaries × 2 architectures"
production_hardening = "DEPLOYED — AUTH_MODE=enforced, BTSP=required"
btsp_13_13 = "ALL 13 PRIMALS — ClientHello shipped"
beardog_152_154 = "FIDO2 + iosGate + HSM agnostic + 3333L debris cleaned"
caddy_301 = "primals.eco → sporeprint.primals.eco (permanent, path-preserving)"
northgate_dns = "FIXED — removed DNS=10.13.37.1 from WG config"
gate_enrollment_3of5 = "southGate + strandGate + westGate WG peers REGISTERED on golgiBody"
phase0_enrollment_endpoint = "LIVE — primals.eco/enroll, drawbridge + Caddy, 3/3 tests PASS"
windows_genomebins = "13/13 — first cross-compile harvest, rhizoCrypt cfg(unix) fix shipped"
primalspring_gate_calibration = "gate count → >= 13, sporePrint known debt added"

[deployment_155d]
depot_primals_eco = "LIVE — Caddy site added on golgiBody, TLS cert acquired, 200 OK"
socket_permissions = "FIXED — membrane group created, sockets 0660 root:membrane, tmpfiles.d persistent"
gate_status = "13/13 primals alive, sovereignty s1+s2+s4 OK, service.crash-loop OK"
caddy_deprecation = "basicauth → basic_auth pending (Caddy warns, still functional)"

[peptidoglycan_prep]
bluegate_anchor = "PREP — blueGate identified as house2 peptidoglycan anchor"
bluegate_lan_candidates = ["192.168.4.200 (Dell)", "192.168.4.231 (Gigabyte)"]
bluegate_flint2 = "192.168.4.250 (GL.iNet = Flint 2 bridge confirmed)"
bluegate_wg_planned = "10.13.37.12 (pending keygen)"
direct_lan_status = "DEGRADED — ICMP to house2 gates fails, backbone path needs audit"
flint2_bridge = "CONFIRMED live at 192.168.4.250, bridges blueGate + swiftGate"
house2_live_hosts = ["192.168.4.200", "192.168.4.214", "192.168.4.218 (ms-device-h2)", "192.168.4.231", "192.168.4.237 (irongate)", "192.168.4.244 (xbox-h2)"]
next_steps = "Identify blueGate LAN IP via RustDesk session, assign DHCP reservation, WG keygen"

[deployment_155h]
step_ca = "LIVE — ca.primals.eco, step-ca v0.30.2, SSH CA enabled, host cert issued for golgiBody"
step_ca_fingerprint = "566b6a6ef9611355c5697973a7c9d699acc06318ef918ce992cd4e7884581cd5"
step_cli_sporeGate = "BOOTSTRAPPED — step-cli v0.30.6 installed"
log_before_drop = "DONE — RUSTDESK_UDP_RATELIMIT + RUSTDESK_TCP_RATELIMIT LOG rules in RUSTDESK_MEMBRANE chain"
glibc_depot_target = "IN PROGRESS — gpu=true added to sources.toml for compute trio, harvest building x86_64-unknown-linux-gnu"
harvest_155h = "12/15 musl built + pushed (155f), glibc harvest running (155h)"

[code_team_flags]
checksums_format = "RESOLVED — new membrane binary handles both formats (155d)"
s3_content_probe = "FLAGGED — gate.status s3_content probe returns 404 (path alignment: code team)"
vcs_parity = "FLAGGED — 21/21 repos drifted (code team sync)"
mesh_socket = "FLAGGED — mesh relay socket not wired on sporeGate (code team: cellMembrane)"

[remaining]
glibc_depot = "P0 — harvest running, compute trio (barraCuda, coralReef, toadStool) need x86_64-unknown-linux-gnu for Vulkan dlopen"
phase1_gate_enrollment = "P0 — 5 gates need enrollment (3 WG-keyed, 2 pending keygen)"
bluegate_anchor_prep = "P0 — identify LAN IP, DHCP reservation, WG keygen, peptidoglycan mgmt plane"
northgate_irongate_rustdesk = "P1 — both LAN-reachable, RustDesk degraded, needs physical access"
bluegate_builder = "P1 — post-enrollment: set MEMBRANE_BUILD_AUTHORITY=1, verify toolchain"
graphenegate_floating = "P2 — evolve from ADB peripheral to autonomous gate"
