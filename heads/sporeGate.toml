# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-25T00:20:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-25T00:35:00Z"
wave = "150x"

[heads]
bearDog = "45a6355"
songBird = "0e0fc38"
nestGate = "d5d45df"
biomeOS = "c0f51e6"
rhizoCrypt = "ae205a8"
squirrel = "68c9e04"
sweetGrass = "76dc35f"
loamSpine = "cb1beb0"
skunkBat = "49da286"
coralReef = "2246ebf"
barraCuda = "3cfdf0e"
petalTongue = "3227ea5"
toadStool = "bfa9632"
sourDough = "3a0b52d"
cellMembrane = "1855216"
primalSpring = "c6f20ca"
wateringHole = "ae160a1"
sporePrint = "67cc325"

[deploy]
songbird_version = "0.2.1+sustained (deployed, crypto composition NOT yet deployed — mid-migration)"
beardog_version = "0.10.0-silicon-atheism (cipher floor + blake3 in source, not yet redeployed)"
petaltongue_version = "1.7.0"
membrane_version = "0.1.0 (eee7e84) — tower.shadow + crash-loop breaker (deployed all 3 gates)"
gateway_status = "SUBDOMAIN_STANDARD_DEPLOYED"
depot_status = "LIVE — 59 binaries × 4 architectures"
mesh_status = "6-GATE MESH — golgi, sporeGate, eastGate, flockGate, ironGate, northGate"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active"

[tower]
status = "3/3 LIVE"
songbird = "LIVE"
beardog = "LIVE"
skunkbat = "LIVE"
mesh_peers = 3
mesh_init = "ExecStartPost dropin (auto-seed + LAN peer.connect for eastGate)"

[tower_shadow]
status = "ACTIVE — gate-agnostic script, /usr/local/bin symlink for root"
interval = "60min"
results = "525 files (shadow timer + manual benchmarks)"
gates_active = ["sporeGate", "flockGate", "golgiBody"]

[benchmark]
phase = "PHASE 1 PASS — Tower EXCEEDS WireGuard"
lan_latency_tower = "0.448ms avg, 0.006ms jitter (50 probes, Jul 25 00:23 UTC)"
lan_latency_wg = "0.60ms avg (direct LAN, same subnet — PARITY)"
wg_overlay_latency = "157.99ms avg, 0.634ms jitter (routes through golgiBody VPS)"
lan_vs_overlay = "Tower LAN 353x faster than WG overlay (0.448ms vs 158ms)"
wan_latency_tower = "137.9ms avg (flockGate WAN)"
wan_latency_wg = "136.3ms avg (flockGate WAN — PARITY)"
lan_discovery_gap = "peer.connect works (LAN TCP registered), but mesh.find_path still resolves overlay. mesh routing doesn't prefer EndpointType::Local — code fix needed in songBird."

[hardware]
lan_peering = "CONFIRMED: eastGate at 192.168.4.244, 0.17ms RTT"
ten_gig = "10G+1G confirmed operational. Sustained throughput testing: songbird benchmark done."
gate_enrollment = "PENDING: southGate + strandGate (USB staged, physical access required)"

[crash_loop_breaker]
status = "OPERATIONAL"
scanned = 14
loops = 0

[primalspring]
head = "c6f20ca"
scenarios = 196
known_debt = "graphenegate-readiness=2, sporeprint-pure-primal-parity=1, + 9 tower stress/pen scenarios"
tests = "1240 passed, 0 failed, 2 ignored"
suite_status = "GREEN"

[surfaces]
sporeprint = "200"
footprint = "200"
live = "200"
webb = "200"
git = "200"
root = "301 (redirect to sporeprint)"

[golgi_health]
load_spike = "2026-07-25T00:17Z — load avg 71.02, recovered to ~17 trailing. No root cause found, no crash. TCP connections stalled during spike but ICMP and WG-UDP unaffected."
note = "All services (forgejo, caddy, songbird) remained active throughout."

[resolved]
tower_parity_p1 = "PHASE 1 PASS (Wave 150w)"
tower_shadow_p0 = "ACTIVE on 3 gates (Wave 150w)"
lan_peering_p0 = "CONFIRMED (Wave 150w)"
crash_loop_breaker = "DEPLOYED (Wave 150x)"
sustained_benchmark = "DONE — Tower 353x LAN (0.45ms vs 158ms), WAN parity (138ms vs 136ms)"
sporeprint_transplant = "DONE"
golgi_shadow_fix = "DONE — golgiBody songbird updated"
shadow_script_fix = "DONE (Wave 150x) — gate-agnostic paths"

[blocked_on_eastgate]
iperf3_server = "P1 — needs iperf3 -s running on eastGate"
ssh_key_auth = "P1 — sporeGate SSH config + pubkey ready, eastGate needs: echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILU4i9hEtHJA02/JZ8XR/OHaR/bSiuAaDRMhdJX7zuRp sporegate-gate-v1' >> ~/.ssh/authorized_keys"
biomeos_beacon = "P1 — phantom unit (11,161 restarts), needs disable"
manifest_ip = "RESOLVED — lan_ip = 192.168.4.244 confirmed in ecosystem_manifest.toml"

[lan_discovery]
status = "PARTIALLY WORKING"
peer_connect = "SUCCESS — peer.connect creates LAN TCP (0.45ms), mesh_registered=true"
mesh_routing = "GAP — mesh.find_path and mesh.peers still resolve overlay address (wireguard://10.13.37.5:7700)"
root_cause = "songBird mesh routing doesn't prefer EndpointType::Local over overlay. Code fix needed."
workaround = "mesh-init.sh issues peer.connect for LAN peers on startup. Direct benchmark via LAN IP works."
impact = "353x latency advantage for direct TCP, but capability.call still routes through WG overlay"
