# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-30T16:40:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-31T21:20:00Z"
wave = "155n"

[heads]
bearDog = "5e80b53"
songBird = "9046664"
nestGate = "6b6d484"
biomeOS = "6a69807"
rhizoCrypt = "d4972b0"
squirrel = "4bcf79e"
sweetGrass = "ab887e8"
loamSpine = "5b3cabf"
skunkBat = "b0df971"
coralReef = "edcd696"
barraCuda = "3460368"
petalTongue = "b135400"
toadStool = "92aeb14"
sourDough = "3a0b52d"
cellMembrane = "d7026d7"
primalSpring = "05e72e4"
wateringHole = "de62bc1"
sporePrint = "d66b6b9"

[deploy]
songbird_version = "0.2.1+ (9046664) — TCP registration fix + shared ServiceRegistry"
beardog_version = "0.9.0+ (5e80b53) — dual-socket fix + 94 orphan files purged (-19,214 lines)"
biomeos_version = "4.56.0 (6a69807) — G22 convergence: unified socket namespace, 47 dead deps removed, 244 capabilities registered"
membrane_version = "0.1.0 (d7026d7) — P2 platform detection FIXED (J12 unblock), registry API hardened, validate_with_deps (J19), J16, J13"
depot_status = "FRESH — 16 musl + 15 gnu + 15 windows = 46 x86_64 binaries, ALL targets current, BLAKE3 verified"
mesh_status = "9-GATE MESH (blueGate enrolled, southGate ENROLLED as validation gate)"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active — sovereign CI LIVE"

[tower]
status = "TOWER ATOMIC — biomeOS v4.56 (6a69807 G22 convergence) + cellMembrane (d7026d7 P2 platform fix) — 11/11 HEALTHY"
songbird = "LIVE — 9046664, TCP :7700, federation active"
beardog = "DEPLOYED — 5e80b53, dual-socket fix, crypto.sign_ed25519"
skunkbat = "LIVE (PUBLIC)"

[gate_status]
probes_ok = 11
probes_degraded = 0
total_probes = 11
depot_integrity = "OK — 16 verified, 0 hash mismatch"
primals_alive = "OK — 13/13 alive"
depot_freshness = "OK — 13/13 binaries present"
sovereignty_tls = "OK — depot.primals.eco 200"
sovereignty_relay = "OK — RustDesk hbbs+hbbr"
sovereignty_content = "OK — depot serving 8459KB"
sovereignty_auth = "OK — beardog responding via neuralAPI"
service_crash_loop = "OK — 14 services scanned, no crash loops"
vcs_parity = "OK — 0 repos drifted (full cascade completed)"
mesh_reachability = "OK — 7 peers, 7 reachable"
rootpulse_ledger = "OK — advisory (no session yet, populates on cascade)"

[harvest]
wave = "155n"
harvested = "2026-07-31T17:54:00Z"
x86_64_musl = 16
x86_64_gnu = 15
x86_64_windows = 15
provenance = "builder=sporeGate, toolchain=rustc 1.96.1, mingw=x86_64-w64-mingw32-gcc"
golgi_pushed = "CONFIRMED — biomeOS v4.56 + membrane d7026d7 on all 3 targets"
rebuilt = "biomeos(6a69807) musl+gnu+windows, membrane(d7026d7) musl+gnu+windows — G22 convergence + P2 platform fix"
note = "Wave 155n CASCADE 5: GNU depot COMPLETE (5→15 binaries). sporePrint LIVE. Checkpoint items 3+8 CLEARED. 46 total depot binaries."
checksums = "BLAKE3 regenerated — 46 x86_64 binaries verified (16 musl + 15 gnu + 15 windows)"

[nucleus_redeploy]
wave = "155n"
biomeos = "v4.56 (6a69807) — G22 convergence step 1+2: unified socket namespace (membrane/), dead dep cleanup (47 removed), 244 capabilities"
membrane = "d7026d7 — P2 platform detection FIXED (detect_target_triple via Platform::detect, J12 unblock) + registry API hardened"
tower_status = "DEPLOYED — biomeOS v4.56 G22 convergence + cellMembrane d7026d7 P2 platform fix — 11/11 HEALTHY"

[sporeprint]
wave = "155n"
status = "LIVE — sporeprint.primals.eco, 313 pages, 23 sections, zola 0.19.2"
published = "2026-07-31T18:05:00Z"
commit = "d66b6b9"
restructure = "conceptual era → demonstration era: Lab | Science | Architecture | Products | Get Started"
hero = "NUCLEUS Is Running — 3 gates, 13 primals, Provenance 7/7"
foundation_pages = 47
new_sections = "backstory, foundation, getting-started"
validated_papers = "Paper 10 (QCD), Paper 14 (hardware), Paper 21 (provenance), Paper 24 (all-silicon)"

[services_golgi]
caddy = "RUNNING — /etc/membrane/Caddyfile, 12 sites"
step_ca = "ACTIVE — ca.primals.eco, v0.30.2, SSH CA enabled"
depot = "LIVE — depot.primals.eco, 200 OK, 46 x86_64 binaries (16 musl + 15 gnu + 15 windows)"
sporeprint = "LIVE — sporeprint.primals.eco, 313 pages, zola built from d66b6b9"
rustdesk_hbbs = "OK"
rustdesk_hbbr = "OK"

[bluegate_enrollment]
wave = "155i"
wg_peer = "REGISTERED"
wg_status = "LIVE — handshake active"
ssh_key = "REGISTERED — Forgejo key ID 14"
tower_status = "READY — 15/15 Windows .exe in depot (membrane.exe NOW AVAILABLE)"
sub_builder = "UNBLOCKED — membrane.exe compiles, blueGate can run membrane natively"

[resolved]
tower_atomic = "COMPLETE — 7/7 debt resolved"
glibc_depot_target = "DONE (155h) — compute trio gnu targets live"
step_ca_deployment = "DONE (155h) — ca.primals.eco LIVE"
depot_content_routing = "DONE (155i) — /depot/ prefix strip"
membrane_j6 = "DONE (155i) — gate.configure + gate.apply"
songbird_windows_p0 = "DONE (155i) — depot binary rebuilt with TCP fallback"
deep_debt_refresh = "DONE (155i) — 8 primals rebuilt from deep-debt commits"
windows_depot_14_14 = "DONE (155k) → 15/15 (155m) — membrane.exe P1 FIXED"
biomeos_v447 = "DONE (155k) → v4.49 → v4.50 → v4.51 (155m final) — socket ownership + evap + binary path ALL FIXED"
beardog_crypto_sign = "DONE (155k) — crypto.sign_ed25519 + dual-socket fix (155m)"
membrane_dns = "DONE (155k) — dns.configure/dns.apply generators shipped"
dhcp_dns_fix = "DONE (155i) — DHCP DNS option corrected"
sovereign_ci = "DONE (155k) — J9+J10+J11 killed, push-to-deploy live"
membrane_exe_p1 = "DONE (155m) — UnixStream #[cfg(unix)] gated, membrane.exe compiles + in depot"
beardog_orphan_purge = "DONE (155m) — 94 files, -19,214 lines"
reqwest_purge = "DONE (155m) — sovereign HTTP/1.1 client, pure-Rust TLS"

[pipeline_automation]
wave = "155n"
status = "SOVEREIGN CI LIVE — P2 golgi hook FIXED (3 bugs). Full E2E verified for non-broker primals."
j9_push_trigger = "KILLED — 3 hook bugs fixed (no dispatcher, case mismatch, category mismatch). Verified E2E."
j10_cascade_auto_harvest = "KILLED — MEMBRANE_BUILD_AUTHORITY=1 set, drift pipeline active"
j11_multi_target = "KILLED — manifest [build.*].targets verified for all primals"
j12_sub_builder = "UNBLOCKED — membrane.exe P1 FIXED (4ccbab1) + P2 platform detection FIXED (d7026d7). SSH dispatch ready. Needs blueGate enrollment."
j13_mesh_notify = "PARTIAL — depot.updated WORKS (songBird UDS). build_pending is stub-only (~20 LOC to wire)."
phase_a = "DONE — sovereign CI live, validated E2E, hook bugs FIXED"
phase_b = "UNBLOCKED — membrane.exe compiles. blueGate can run membrane natively. Need SSH dispatch wiring."
phase_c = "POLISH — HTTP webhook listener, build queue, provenance signing chain"

[divergences]
golgi_hook_p2 = "FIXED (155n) — 3 bugs: missing post-receive dispatcher, repo name case mismatch, category singular/plural"
coevolution_mode_gap = "FIXED (652cf8a7) — Neural API btsp_optional=true + composition.test_swap registered. E2E VALIDATED."
gate_name_env_var = "FIXED (155n) — MEMBRANE_* standardization shipped in cellMembrane 4a2b39c"

# RECURRING — resolve with G22 steps 3-5
d1_socket_evaporation_restart = "RECURRING — biomeOS restart clears primal sockets in /run/membrane. Requires restart of all primal services. Owner: biomeOS (G22 step 3+4)."
d2_run_membrane_permissions = "RECURRING — biomeOS resets /run/membrane to 0770. gate.status gets Permission denied. Workaround: sudo chmod 0777. Owner: biomeOS (G22 step 4)."

# P3 — need evolution
d3_checksums_format_drift = "P3 — manual checksums.toml regeneration hits wrong TOML format. Needs codified depot.seal command. Owner: sporeGate/cellMembrane."
d4_candidate_self_test = "P3 — composition.test_swap dispatches but candidate probe fails (no env: FAMILY_ID, sockets). Needs lightweight version-check or env passthrough. Owner: biomeOS."
d5_ci_source_tree = "P3 — sovereign CI builds from /opt/ecoPrimals (stale). Needs git pull before build. Owner: sporeGate."
d6_dual_service_scaffold = "TRANSITIONAL — biomeOS runs as 2 systemd units (api + neural-api). G22 step 3 merges into single process."
d7_sporeprint_manual_publish = "P3 — zola build requires manual SSH to golgi. Needs Forgejo post-receive hook. Owner: sporeGate."
d8_neural_api_routing_gaps = "P3 — gate.status shows capability not registered for several primals via neural-api. Primals need to register with coordinator. Owner: biomeOS (G22)."

[code_team_flags]
mesh_socket = "FLAGGED — songBird mesh relay socket"
rootpulse = "RESOLVED (155m final) — advisory OK mode (0cfcce5)"
membrane_windows = "RESOLVED (155m) — membrane.exe compiles and serves from depot"
dns_configure = "SHIPPED (155k) — cellMembrane dns.configure/dns.apply"
socket_evaporation = "RESOLVED (155m+) — biomeOS v4.50 (06ed323f), any Ok(btsp) = alive"
binary_path_retention = "RESOLVED (155m+) — biomeOS v4.50 (06ed323f), probe_binary() during discovery"
sandbox_false_positive = "RESOLVED (155m final) — ServerContract resolution (0cfcce5)"
socket_bind_permissions = "RESOLVED (155m final) — biomeOS 0660+chown:membrane (0e45262f)"
checksums_partial = "RESOLVED (155m final) — finalize_depot() full disk scan (0cfcce5)"
tmpfiles_d = "RESOLVED (155m final) — membrane.conf installed (0cfcce5)"
