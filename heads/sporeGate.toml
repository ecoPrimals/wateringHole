# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-30T13:00:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-30T13:45:00Z"
wave = "155k"

[heads]
bearDog = "d6b1003"
songBird = "d9bda55"
nestGate = "6b6d484"
biomeOS = "4e8f00c"
rhizoCrypt = "d4972b0"
squirrel = "a9493a8"
sweetGrass = "ab887e8"
loamSpine = "d79231a"
skunkBat = "b0df971"
coralReef = "edcd696"
barraCuda = "3460368"
petalTongue = "f61c808"
toadStool = "2df7139"
sourDough = "3a0b52d"
cellMembrane = "2b82722"
primalSpring = "05e72e4"
wateringHole = "0bf147c"
sporePrint = "67cc325"

[deploy]
songbird_version = "0.2.1 (d9bda55) — 3 Windows compile fixes + deep debt"
beardog_version = "0.9.0 (d6b1003) — crypto.sign_ed25519 real signing + Windows platform gating"
biomeos_version = "4.47 (4e8f00c) — NUCLEUS orchestrator, riboCipher fix, socket unification, composition lifecycle"
membrane_version = "0.1.0 (2b82722) — dns.configure/dns.apply + magic number centralization"
depot_status = "FRESH — 16 musl + 3 gnu + 14 windows binaries, ALL targets current, BLAKE3 verified"
mesh_status = "9-GATE MESH (blueGate enrolled, handshake active)"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active — sovereign CI LIVE"

[tower]
status = "TOWER ATOMIC LIVE — 3/3 restarted with Wave 155k binaries"
songbird = "LIVE — 0.2.1, TCP :7700, federation active"
beardog = "LIVE — 0.9.0, crypto.sign_ed25519 REAL SIGNING (d6b1003)"
skunkbat = "LIVE (PUBLIC)"

[gate_status]
probes_ok = 6
probes_degraded = 5
total_probes = 11
depot_integrity = "OK — 16 verified, 0 hash mismatch"
primals_alive = "DEGRADED — primals running but probe needs shell group refresh"
depot_freshness = "DEGRADED — workspace plasmidBin path mismatch (binaries current in /opt/ecoPrimals/depot)"
sovereignty_tls = "OK — depot.primals.eco 200"
sovereignty_relay = "OK — RustDesk hbbs+hbbr"
sovereignty_content = "OK — depot serving 8543KB"
sovereignty_auth = "OK — beardog responding via neuralAPI"
service_crash_loop = "OK — 14 services scanned, no crash loops"
vcs_parity = "DEGRADED — needs full cascade (petalTongue, barraCuda pulled, others pending)"
mesh_reachability = "DEGRADED — songbird socket perm (shell group not refreshed)"
rootpulse_ledger = "DEGRADED — not yet implemented (cellMembrane)"

[harvest]
wave = "155k"
harvested = "2026-07-30T12:50:00Z"
x86_64_musl = 16
x86_64_gnu = 3
x86_64_windows = 14
provenance = "builder=sporeGate, toolchain=rustc 1.96.1, mingw=x86_64-w64-mingw32-gcc"
golgi_pushed = "CONFIRMED — all 3 targets synced"
rebuilt = "beardog(d6b1003), biomeos(4e8f00c), toadstool(2df7139), coralreef(edcd696), membrane(2b82722) for musl+gnu+windows"
note = "Wave 155k: 3 previously blocked Windows .exe now compile (beardog, toadstool, coralreef). biomeOS v4.47 NUCLEUS orchestrator. bearDog crypto.sign_ed25519. cellMembrane dns.configure/dns.apply."
checksums = "BLAKE3 regenerated — 33 binaries verified (16 musl + 3 gnu + 14 windows)"

[nucleus_redeploy]
wave = "155k"
biomeos = "v4.47 (4e8f00c) — NUCLEUS orchestrator deployed, COORDINATED MODE active"
beardog = "0.9.0 (d6b1003) — crypto.sign_ed25519 real signing deployed"
membrane = "0.1.0 (2b82722) — dns.configure/dns.apply installed locally + depot"
tower_status = "LIVE — beardog + songbird + skunkbat restarted with new binaries"

[services_golgi]
caddy = "RUNNING — /opt/membrane/caddy, 12 sites, /depot/ prefix routing"
step_ca = "ACTIVE — ca.primals.eco, v0.30.2, SSH CA enabled"
depot = "LIVE — depot.primals.eco, 200 OK, all 3 target dirs serving"
rustdesk_hbbs = "OK"
rustdesk_hbbr = "OK"

[bluegate_enrollment]
wave = "155i"
wg_peer = "REGISTERED"
wg_status = "LIVE — handshake active"
ssh_key = "REGISTERED — Forgejo key ID 14"
tower_status = "READY — all Windows .exe now in depot (beardog, toadstool, coralreef unblocked)"
sub_builder = "READY — G10 goal, blueGate as Windows build accelerator"

[resolved]
tower_atomic = "COMPLETE — 7/7 debt resolved"
glibc_depot_target = "DONE (155h) — compute trio gnu targets live"
step_ca_deployment = "DONE (155h) — ca.primals.eco LIVE"
depot_content_routing = "DONE (155i) — /depot/ prefix strip"
membrane_j6 = "DONE (155i) — gate.configure + gate.apply"
songbird_windows_p0 = "DONE (155i) — depot binary rebuilt with TCP fallback"
deep_debt_refresh = "DONE (155i) — 8 primals rebuilt from deep-debt commits"
windows_depot_14_14 = "DONE (155k) — ALL 14 Windows .exe compile and serve (beardog, toadstool, coralreef unblocked)"
biomeos_v447 = "DONE (155k) — NUCLEUS orchestrator deployed (riboCipher fix, socket unification, composition lifecycle)"
beardog_crypto_sign = "DONE (155k) — crypto.sign_ed25519 real signing deployed (Provenance 7/7 unblock)"
membrane_dns = "DONE (155k) — dns.configure/dns.apply generators shipped (2b82722)"
dhcp_dns_fix = "DONE (155i) — DHCP DNS option corrected"

[pipeline_automation]
wave = "155k"
status = "SOVEREIGN CI LIVE — J9+J10+J11 killed. Push-to-deploy automated. Validated E2E."
j9_push_trigger = "KILLED — SSH key plumbed, hook fires, sovereign.ci.trigger validated E2E"
j10_cascade_auto_harvest = "KILLED — MEMBRANE_BUILD_AUTHORITY=1 set, drift pipeline active"
j11_multi_target = "KILLED — manifest [build.*].targets verified for all primals"
j12_sub_builder = "BLOCKED — blueGate NUCLEUS not live + membrane.exe won't compile (code team P1). SSH dispatch pattern ready."
j13_mesh_notify = "PARTIAL — depot.updated WORKS (songBird UDS). build_pending is stub-only (~20 LOC to wire)."
phase_a = "DONE — SSH key + env var + membrane binary + cargo symlinks + /etc/environment. Validated: squirrel build+sandbox+push E2E"
phase_b = "WAITING — blueGate NUCLEUS live → SSH dispatch for Windows builds → J12 killed"
phase_c = "POLISH — HTTP webhook listener, build queue, provenance signing chain"

[code_team_flags]
mesh_socket = "FLAGGED — songBird mesh relay socket"
rootpulse = "FLAGGED — cellMembrane rootpulse not implemented"
membrane_windows = "P1 — UnixStream + handshake_async not #[cfg(unix)] gated (membrane.exe still blocked)"
dns_configure = "SHIPPED (155k) — cellMembrane dns.configure/dns.apply in 2b82722"
