# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-30T16:40:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-30T17:10:00Z"
wave = "155m"

[heads]
bearDog = "5e80b53"
songBird = "9046664"
nestGate = "6b6d484"
biomeOS = "b290cc8"
rhizoCrypt = "d4972b0"
squirrel = "a9493a8"
sweetGrass = "ab887e8"
loamSpine = "5b3cabf"
skunkBat = "b0df971"
coralReef = "edcd696"
barraCuda = "3460368"
petalTongue = "71c95c7"
toadStool = "92aeb14"
sourDough = "3a0b52d"
cellMembrane = "4e77ffd"
primalSpring = "05e72e4"
wateringHole = "428c3d1"
sporePrint = "67cc325"

[deploy]
songbird_version = "0.2.1+ (9046664) — TCP registration fix + shared ServiceRegistry"
beardog_version = "0.9.0+ (5e80b53) — dual-socket fix + 94 orphan files purged (-19,214 lines)"
biomeos_version = "4.50 (b290cc8) — socket evaporation FIXED + binary path retention FIXED"
membrane_version = "0.1.0 (4e77ffd) — reqwest purged, sovereign HTTP client, steamGate user-space, membrane.exe FIXED"
depot_status = "FRESH — 16 musl + 4 gnu + 15 windows = 35 x86_64 binaries, ALL targets current, BLAKE3 verified"
mesh_status = "9-GATE MESH (blueGate enrolled, handshake active)"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active — sovereign CI LIVE"

[tower]
status = "TOWER ATOMIC — biomeOS v4.50 + bearDog 155m deployed"
songbird = "LIVE — 9046664, TCP :7700, federation active"
beardog = "DEPLOYED — 5e80b53, dual-socket fix, crypto.sign_ed25519"
skunkbat = "LIVE (PUBLIC)"

[gate_status]
probes_ok = 10
probes_degraded = 1
total_probes = 11
depot_integrity = "OK — 16 verified, 0 hash mismatch"
primals_alive = "OK — 13/13 alive"
depot_freshness = "OK — 13/13 binaries present"
sovereignty_tls = "OK — depot.primals.eco 200"
sovereignty_relay = "OK — RustDesk hbbs+hbbr"
sovereignty_content = "OK — depot serving 8459KB"
sovereignty_auth = "OK — beardog responding via neuralAPI"
service_crash_loop = "OK — 14 services scanned, no crash loops"
vcs_parity = "OK — 0 repos drifted (full cascade completed)"
mesh_reachability = "OK — 3 peers, 3 reachable"
rootpulse_ledger = "DEGRADED — not yet implemented (cellMembrane)"

[harvest]
wave = "155m"
harvested = "2026-07-30T16:35:00Z"
x86_64_musl = 16
x86_64_gnu = 4
x86_64_windows = 15
provenance = "builder=sporeGate, toolchain=rustc 1.96.1, mingw=x86_64-w64-mingw32-gcc"
golgi_pushed = "CONFIRMED — all 3 targets synced + membrane.exe NEW"
rebuilt = "beardog(5e80b53), biomeos(b290cc8), toadstool(92aeb14), petaltongue(71c95c7), membrane(4e77ffd) for musl+gnu+windows"
note = "Wave 155m+: biomeOS v4.50 live pipeline test. P2 socket evap + binary path FIXED. Depot 35 x86_64 (gnu biomeOS added). sandbox::validate divergence found."
checksums = "BLAKE3 regenerated — 35 x86_64 binaries verified (16 musl + 4 gnu + 15 windows)"

[nucleus_redeploy]
wave = "155m"
biomeos = "v4.50 (b290cc8) — socket evaporation FIXED + binary path retention FIXED, deployed via systemd"
beardog = "0.9.0+ (5e80b53) — dual-socket fix + orphan purge, deployed"
membrane = "0.1.0 (4e77ffd) — reqwest purged, sovereign HTTP, steamGate user-space, membrane.exe P1 FIXED"
tower_status = "DEPLOYED — biomeOS v4.50 + bearDog 155m + songBird TCP fix"

[services_golgi]
caddy = "RUNNING — /opt/membrane/caddy, 12 sites"
step_ca = "ACTIVE — ca.primals.eco, v0.30.2, SSH CA enabled"
depot = "LIVE — depot.primals.eco, 200 OK, 35 x86_64 binaries (16 musl + 4 gnu + 15 windows)"
rustdesk_hbbs = "OK"
rustdesk_hbbr = "OK"

[bluegate_enrollment]
wave = "155i"
wg_peer = "REGISTERED"
wg_status = "LIVE — handshake active"
ssh_key = "REGISTERED — Forgejo key ID 14"
tower_status = "READY — 15/15 Windows .exe in depot (membrane.exe NOW AVAILABLE)"
sub_builder = "UNBLOCKED — membrane.exe compiles, blueGate can run membrane natively"

[resolved]
tower_atomic = "COMPLETE — 7/7 debt resolved"
glibc_depot_target = "DONE (155h) — compute trio gnu targets live"
step_ca_deployment = "DONE (155h) — ca.primals.eco LIVE"
depot_content_routing = "DONE (155i) — /depot/ prefix strip"
membrane_j6 = "DONE (155i) — gate.configure + gate.apply"
songbird_windows_p0 = "DONE (155i) — depot binary rebuilt with TCP fallback"
deep_debt_refresh = "DONE (155i) — 8 primals rebuilt from deep-debt commits"
windows_depot_14_14 = "DONE (155k) → 15/15 (155m) — membrane.exe P1 FIXED"
biomeos_v447 = "DONE (155k) → v4.49 (155m) → v4.50 (155m+) — socket evap + binary path FIXED"
beardog_crypto_sign = "DONE (155k) — crypto.sign_ed25519 + dual-socket fix (155m)"
membrane_dns = "DONE (155k) — dns.configure/dns.apply generators shipped"
dhcp_dns_fix = "DONE (155i) — DHCP DNS option corrected"
sovereign_ci = "DONE (155k) — J9+J10+J11 killed, push-to-deploy live"
membrane_exe_p1 = "DONE (155m) — UnixStream #[cfg(unix)] gated, membrane.exe compiles + in depot"
beardog_orphan_purge = "DONE (155m) — 94 files, -19,214 lines"
reqwest_purge = "DONE (155m) — sovereign HTTP/1.1 client, pure-Rust TLS"

[pipeline_automation]
wave = "155m"
status = "SOVEREIGN CI LIVE — J9+J10+J11 killed. J12 UNBLOCKED (membrane.exe fixed)."
j9_push_trigger = "KILLED — SSH key plumbed, hook fires, sovereign.ci.trigger validated E2E"
j10_cascade_auto_harvest = "KILLED — MEMBRANE_BUILD_AUTHORITY=1 set, drift pipeline active"
j11_multi_target = "KILLED — manifest [build.*].targets verified for all primals"
j12_sub_builder = "UNBLOCKED — membrane.exe P1 FIXED (4ccbab1). SSH dispatch pattern ready. Needs blueGate NUCLEUS."
j13_mesh_notify = "PARTIAL — depot.updated WORKS (songBird UDS). build_pending is stub-only (~20 LOC to wire)."
phase_a = "DONE — sovereign CI live, validated E2E"
phase_b = "UNBLOCKED — membrane.exe compiles. blueGate can run membrane natively. Need SSH dispatch wiring."
phase_c = "POLISH — HTTP webhook listener, build queue, provenance signing chain"

[code_team_flags]
mesh_socket = "FLAGGED — songBird mesh relay socket"
rootpulse = "FLAGGED — cellMembrane rootpulse not implemented"
membrane_windows = "RESOLVED (155m) — membrane.exe compiles and serves from depot"
dns_configure = "SHIPPED (155k) — cellMembrane dns.configure/dns.apply"
socket_evaporation = "RESOLVED (155m+) — biomeOS v4.50 (06ed323f), any Ok(btsp) = alive"
binary_path_retention = "RESOLVED (155m+) — biomeOS v4.50 (06ed323f), probe_binary() during discovery"
sandbox_false_positive = "P2 NEW — sandbox::validate can't health-check broker primals (biomeOS). Needs orchestrator bypass."
socket_bind_permissions = "P2 KNOWN — primals bind sockets 0600, breaks cross-user probes. Needs umask-aware bind."
