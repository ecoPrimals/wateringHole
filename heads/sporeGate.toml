# Auto-published by membrane temporal.cascade --publish-freshness
# Gate: sporeGate | Updated: 2026-07-29T14:10:00Z
#
# This file is owned exclusively by sporeGate. No other gate writes here.

[meta]
gate = "sporeGate"
updated = "2026-07-29T14:10:00Z"
wave = "155i"

[heads]
bearDog = "78b81e4"
songBird = "c4c5d2d"
nestGate = "263e68a"
biomeOS = "4667f58"
rhizoCrypt = "d4972b0"
squirrel = "a9493a8"
sweetGrass = "076337c"
loamSpine = "e1a8749"
skunkBat = "a872c84"
coralReef = "94ee28a"
barraCuda = "ceca2b1"
petalTongue = "f61c808"
toadStool = "40d0ae3"
sourDough = "3a0b52d"
cellMembrane = "8d9bb58"
primalSpring = "05e72e4"
wateringHole = "b8ca9f7"
sporePrint = "67cc325"

[deploy]
songbird_version = "0.2.1 (c4c5d2d) — GateEnroll dispatch + drawbridge body-method fallback"
beardog_version = "0.9.0 (78b81e4) — FIDO2 + iosGate + HSM agnostic (Wave 152-155i)"
rhizocrypt_fix = "d4972b0 — Windows cross-compile (cfg(unix) gate on publish_capability_manifest)"
petaltongue_version = "1.7.0"
membrane_version = "0.1.0 (8d9bb58) — gate.configure + gate.apply (J6 CLOSED)"
gateway_status = "ENROLLMENT_ENDPOINT_LIVE"
depot_status = "FRESH — 16 musl + 3 gnu binaries, BLAKE3 checksums verified, all 0 hash mismatches"
mesh_status = "8-GATE MESH (5 enrolling)"
build_authority = "MEMBRANE_BUILD_AUTHORITY=1 active"

[tower]
status = "TOWER ATOMIC COMPLETE — 3/3 LIVE, ENROLLMENT ENDPOINT DEPLOYED"
songbird = "LIVE — GateEnroll wired, drawbridge /enroll active, Caddy TLS proxy"
beardog = "LIVE — AUTH_MODE=enforced, FIDO2+iosGate shipped (Wave 154)"
skunkbat = "LIVE (PUBLIC)"

[enrollment_endpoint]
status = "PHASE 0 COMPLETE — LIVE ON GOLGI"
drawbridge_addr = "127.0.0.1:7780"
caddy_route = "primals.eco/enroll → reverse_proxy 127.0.0.1:7780"
method = "mesh.gate_enroll"

[mesh_enrollment]
wave = "155a"
golgi_peers = 8
registered = ["southGate (10.13.37.9)", "strandGate (10.13.37.10)", "westGate (10.13.37.11)"]
pending_keygen = ["blueGate (10.13.37.12)", "swiftGate (10.13.37.13)"]
forgejo_ssh = "strandGate key registered (ID 12)"

[tower_shadow]
status = "ACTIVE — hourly data"
interval = "60min"
gates_active = ["sporeGate", "flockGate", "golgiBody"]

[benchmark]
phase = "PHASE 1 PASS — Tower EXCEEDS WireGuard"
lan_tower = "0.454ms avg (353x vs WG overlay)"
wan_tower = "76.8ms avg, 1.7x sustained"
iperf3_lan = "2.36 Gbps sporeGate→eastGate (CONFIRMED)"

[harvest]
wave = "155i"
harvested = "2026-07-29T14:06:00Z"
x86_64_musl = 16
x86_64_gnu = 3
provenance = "builder=sporeGate, toolchain=rustc 1.96.1"
golgi_pushed = "CONFIRMED — musl + gnu targets synced to depot.primals.eco"
note = "Full compute trio glibc harvest + all primals refreshed from latest source"
checksums = "BLAKE3 regenerated — 16 musl + 3 gnu verified, 0 mismatches"

[gate_status]
probes_ok = 9
probes_degraded = 2
total_probes = 11
depot_integrity = "OK — 16 verified, 0 hash mismatch"
primals_alive = "OK — 13/13"
depot_freshness = "OK — oldest 3h"
sovereignty_tls = "OK — depot.primals.eco 200"
sovereignty_relay = "OK — RustDesk hbbs+hbbr operational"
sovereignty_content = "OK — depot serving 8163KB"
sovereignty_auth = "OK — beardog responding"
service_crash_loop = "OK — 14 services, no crash loops"
vcs_parity = "OK — 0 drifted"
mesh_reachability = "DEGRADED — mesh relay socket not found (code team: songBird)"
rootpulse_ledger = "DEGRADED — not yet implemented (code team: cellMembrane)"

[services_golgi]
caddy = "RUNNING — /opt/membrane/caddy, 12 sites"
step_ca = "ACTIVE — ca.primals.eco, v0.30.2, SSH CA enabled"
depot = "LIVE — depot.primals.eco, /depot/ prefix routing FIXED (155i)"
rustdesk_hbbs = "OK"
rustdesk_hbbr = "OK"

[hardening]
beardog_auth_mode = "enforced (was Permissive)"
beardog_uds_btsp = "required (was OFF)"
method = "systemd drop-in override"
log_before_drop = "DONE — RUSTDESK_UDP_RATELIMIT + RUSTDESK_TCP_RATELIMIT LOG rules"

[credentials]
gsc_service_account = "DEPLOYED to golgi:/opt/ecoPrimals/credentials/gsc-service-account.json (chmod 600)"

[primalspring]
head = "05e72e4"
scenarios = 197
known_debt = "3 (grapheneGate provenance 2, sporePrint composition 1)"
suite_status = "GREEN — 1241 passed, 0 failed"

[surfaces]
sporeprint = "200"
footprint = "200"
live = "200"
webb = "200"
git = "200"
root = "301"
enroll = "200 (JSON-RPC param validation)"
depot = "200"
ca = "200"

[resolved]
tower_atomic = "COMPLETE — 7/7 debt resolved"
depot_divergence = "RESOLVED — 16 musl + 3 gnu binaries"
production_hardening = "DEPLOYED — AUTH_MODE=enforced, BTSP=required"
btsp_13_13 = "ALL 13 PRIMALS — ClientHello shipped"
caddy_301 = "primals.eco → sporeprint.primals.eco (permanent, path-preserving)"
gate_enrollment_3of5 = "southGate + strandGate + westGate WG peers REGISTERED"
phase0_enrollment_endpoint = "LIVE — primals.eco/enroll, drawbridge + Caddy"
windows_genomebins = "13/13 — first cross-compile harvest"
glibc_depot_target = "DONE (155h) — barraCuda + coralReef + toadStool x86_64-unknown-linux-gnu built and pushed"
step_ca_deployment = "DONE (155h) — ca.primals.eco LIVE, SSH CA enabled, sshd configured on golgiBody"
depot_content_routing = "DONE (155i) — /depot/ prefix strip added to depot.primals.eco Caddy config"
checksums_refresh = "DONE (155i) — BLAKE3 checksums regenerated for all 19 binaries"
membrane_j6 = "DONE (155i) — gate.configure + gate.apply operational in depot binary"

[deployment_155i]
membrane_rebuild = "DONE — gate.configure/gate.apply operational (cellMembrane 8d9bb58)"
full_harvest = "DONE — 16 musl + 3 gnu binaries rebuilt from latest source, pushed to golgiBody"
depot_routing_fix = "DONE — /depot/ prefix handler added to depot.primals.eco Caddy"
checksums_refresh = "DONE — BLAKE3 regenerated, 0 mismatches"
gate_status_improvement = "5/11 → 9/11 OK probes (depot.integrity, depot.freshness, s3_content, vcs.parity all FIXED)"
nestgate_binary = "DONE — freshly built from commit 263e68a3 (was 15d stale)"

[code_team_flags]
mesh_socket = "FLAGGED — mesh relay socket not wired on sporeGate (songBird)"
rootpulse = "FLAGGED — no rootpulse session (cellMembrane)"
biomeos_btsp_broker = "P0 — BTSP session propagation in signal graph executor (biomeOS)"
biomeos_ribocipher = "P0 — riboCipher transport framing in CLI paths (biomeOS)"

[peptidoglycan_prep]
bluegate_anchor = "PREP — blueGate identified as house2 peptidoglycan anchor"
bluegate_wg_planned = "10.13.37.12 (pending keygen)"
