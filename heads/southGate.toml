# Gate: southGate | LAN GOSSIP VALIDATED — Wave 157j
# 14/14 GREEN. Tower Atomic gossip LIVE on LAN. 4 swarmVine peers, 9 mesh peers.
# Previous "topology blocked" conclusion CORRECTED — stale peer addresses, not network.

[meta]
gate = "southGate"
updated = "2026-08-12T00:09:00Z"
wave = "157j"
role = "VALIDATION GATE + PERFORMANCE CANARY"
posture = "LAN gossip VALIDATED. Tower Atomic mesh live: 9 songBird peers, 4 swarmVine gossip peers. No WG required for same-LAN."

[heads]
bearDog = "9f9b643"
songBird = "a3400d6"
skunkBat = "c8bcf9b"
toadStool = "7980b42"
barraCuda = "a1b9802"
coralReef = "0d32d17"
nestGate = "60ee88d"
rhizoCrypt = "89d912c"
loamSpine = "cb4dc56"
sweetGrass = "cdabdad"
biomeOS = "b3ab3b2"
squirrel = "9ef3ca3"
petalTongue = "9069865"
cellMembrane = "0e13e18"
wateringHole = "d249a14"
hotSpring = "4dbd778"
primalSpring = "7409343"
wetSpring = "6630a52"
lithoSpore = "4ac6cd1"

[hardware]
cpu = "AMD Ryzen 7 5800X3D (8c/16t, 3D V-Cache)"
gpu = "NVIDIA GeForce RTX 4060 (8GB, Ada Lovelace, SHADER_F64)"
ram_gb = 128
os = "Pop!_OS 22.04 LTS"

[deploy]
biomeos_version = "4.57.0"
beardog_version = "0.9.0"
songbird_version = "0.2.1"
barracuda_version = "0.4.0"
toadstool_version = "0.2.0"
nestgate_version = "0.5.0"
swarmvine_deployed = true
depot_source = "depot.primals.eco (Tier 2 rebuild — MeshRelay + G72 Tier 2)"
depot_sync_date = "2026-08-11"
tier2_changes = "bearDog -25% (bloat fixed, 11.7→8.8MB), petalTongue -19% (axum 0.8, 30→24MB), songBird +2% (MeshRelay)"
binary_count = 17

[identity]
family_id = "89df7a2d"
family_name = "southgate-sovereign"
family_seed = "PRESENT"
node_id = "southgate"

[ssh_discipline]
access_model = "Forgejo-only. K-Derm relay chain enforced."

[nucleus]
status = "LIVE — 14/14 (13 NUCLEUS + swarmVine), 45 sockets, 102MB RSS"
composition = "full + swarmVine"
total_rss_mb = 102
process_leak = "FIXED — 0 orphans (coralReef RAII). Exactly 14 processes."
btsp_enforcement = "ACTIVE"

[swarmvine]
status = "GOSSIP LIVE"
gate_id = "southGate"
tcp_port = 7800
gossip_status = "tower_entries=1, peer_count=4, total_ingested=39, entries_sent=4"
cross_gate_peers = 4
lan_peers = "192.168.4.149:7800, 192.168.4.3:7800, 192.168.4.244:7800, 192.168.4.169:7800"
blocker = "NONE — gossip flowing on LAN"

[meshrelay]
status = "LIVE — Tower Atomic mesh connected to LAN peers"
gossip_inject = "WORKS — status:injected"
gossip_relay = "WORKS — relayed_to:local"
gossip_subscribe = "AVAILABLE — needs primal_id registration"
gossip_spread = "TIMEOUT — bidirectional handshake pending (topology team item)"
cross_gate_status = "CONNECTED — 9 mesh peers, 4 gossip peers on LAN. No WG needed."
mesh_peers = 9
gossip_peers = 4

[performance_canary]
beardog_conn_per_sec = 18300
beardog_latency_avg_ms = 0.055
multi_primal_sockets_tested = 29
multi_primal_avg_ms = 0.103
multi_primal_min_ms = 0.051
multi_primal_max_ms = 0.196
process_count = 14
regression_detected = false
benchmark_date = "2026-08-11 (Tier 2 + MeshRelay)"

[performance_canary.history]
wave_157a = "17,314 conn/s, 0.058ms (G68)"
wave_157e = "17,595 conn/s, 0.057ms (Phase 2)"
wave_157i_g72 = "19,733 conn/s, 0.051ms (G72 initial)"
wave_157i_tier2 = "18,300 conn/s, 0.055ms (Tier 2 + MeshRelay)"

[gpu]
status = "HEALTHY — RTX 4060, Vulkan, SHADER_F64 native"

[enmeshment]
readiness = "11/11 — LAN gossip VALIDATED. Tower Atomic mesh operational."
resolved = "LAN peer connectivity confirmed, stale WG addresses bypassed, swarmVine gossip peers active, MeshRelay operational"
remaining = "node_id hostname correction (pop-os→southGate), gossip.spread bidirectional handshake, mDNS auto-discovery"
gossip_surface = "gossip.inject + gossip.relay + gossip.subscribe LIVE. swarmVine epidemic sweep active (4 peers)."
correction = "Previous 'topology blocked' was WRONG — issue was stale peer addresses from WG era, not physical separation."

[validation]
lan_gossip = "VALIDATED — 4 swarmVine peers connected, gossip entries sent, 0 rejected"
mesh_connectivity = "VALIDATED — 9 songBird mesh peers, 7 direct edges, all reachable"
meshrelay = "VALIDATED — gossip.inject returns injected, gossip.relay returns relayed"
tier2_binaries = "DEPLOYED — bearDog -25%, petalTongue -19%, songBird +MeshRelay"
process_leak = "CLOSED — 0 orphans after 75+ min runtime"
canary = "PASS — 18,300 conn/s, 0.055ms avg, no regression"

[observations]
lan_gossip_validated = "Tower Atomic gossip works on LAN without WireGuard. peer.connect with correct IPs → instant mesh."
stale_registry_root_cause = "songBird loaded WG-era IPs (192.168.1.x, 10.0.0.x) from head files. Actual gates at 192.168.4.x/22."
swarmvine_auto_discovery = "swarmVine auto-discovers gossip peers through songBird mesh fabric. No manual config needed once mesh connected."
node_id_issue = "songBird uses hostname (pop-os) not gate name. Fix: set hostname or add --node-id flag."
beardog_shrink = "bearDog binary -25% (11.7→8.8MB) — debug symbol/bloat investigation resolved."
petaltongue_shrink = "petalTongue -19% (30→24MB) — axum 0.7→0.8 + hardcoding elimination."
next_steps = "Topology team: update peer registries with LAN IPs, enable mDNS, fix node_id. gossip.spread handshake needs investigation."
