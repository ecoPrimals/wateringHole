# Gate: southGate | POST-THRESHOLD — Wave 155n
# Validation gate PROVEN. GPU compute ACTIVE. arXiv data PRODUCED.
# NUCLEUS 27.8h stable. Own genetic lineage (89df7a2d).

[meta]
gate = "southGate"
updated = "2026-08-01T21:10:00Z"
wave = "155n-post-threshold"
role = "VALIDATION GATE + COMPUTE NODE + ARXIV DATA PRODUCTION"
posture = "Post-threshold. GPU HMC benchmark complete. arXiv Section 3.4 data ready."

[heads]
bearDog = "5e80b53"
songBird = "9046664"
nestGate = "6b6d484"
biomeOS = "7ccd8ae"
rhizoCrypt = "4716bf5"
squirrel = "4bcf79e"
sweetGrass = "4b5167b"
loamSpine = "5b3cabf"
skunkBat = "b0df971"
coralReef = "edcd696"
barraCuda = "d2ccce4"
petalTongue = "b135400"
toadStool = "92aeb14"
cellMembrane = "d350601"
plasmidBin = "f0d5432"
wateringHole = "93052bd"
hotSpring = "4dbd778"
primalSpring = "1cfee8c"

[hardware]
cpu = "AMD Ryzen 7 5800X3D (8c/16t, 3D V-Cache)"
gpu = "NVIDIA GeForce RTX 4060 (8GB, Ada Lovelace, SHADER_F64)"
gpu_vulkan = "Vulkan 1.4.312, NVIDIA 580.126.18"
ram_gb = 128
storage = "1TB NVMe (479GB free)"
os = "Pop!_OS 22.04 LTS"
driver = "NVIDIA 580.126.18, CUDA 13.0"

[deploy]
biomeos_version = "4.55.0"
beardog_version = "0.9.0"
songbird_version = "0.2.1"
barracuda_version = "0.4.0"
toadstool_version = "0.2.0"
squirrel_version = "0.1.0"
rhizocrypt_version = "0.14.x"
depot_source = "depot.primals.eco (sovereign HTTPS — NO mesh required)"
binary_count = 16
binary_target = "x86_64-unknown-linux-musl (NUCLEUS) + x86_64-unknown-linux-gnu (GPU compute)"
install_path = "~/.local/bin/"
gpu_binary = "~/.local/bin/gnu/barracuda (dynamically linked for Vulkan dlopen)"

[identity]
family_id = "89df7a2d"
family_name = "southgate-sovereign"
lineage = "OWN — not inherited from eastGate family"
beacon_seed = "GENERATED — bearDog entropy + openssl derive"
node_id = "southgate"

[mesh]
wg_status = "DELIBERATELY OFF — validation gate proves non-mesh deployment"
forgejo_ssh = "LIVE — authenticated as golgiAdmin via git.primals.eco:2222 (public DNS)"
federation = "songBird rejecting unknown_family peers on LAN — trust boundary ACTIVE"

[nucleus]
status = "LIVE — 13/13 processes, 32 sockets, 86MB RSS, 27.8h uptime"
composition = "full"
boot_order = "Tower → Nest → Node → biomeOS (dependency-aware)"
total_rss_mb = 86
btsp_enforcement = "ACTIVE — all primals reject plain JSON-RPC on TCP and UDS"
ipc_latency_ms = 0.1
uptime_hours = 27.8

[nucleus.primals]
beardog = "HEALTHY — alive v0.9.0, security-89df7a2d.sock"
songbird = "HEALTHY — alive, federation rejecting unknown_family (CORRECT)"
skunkbat = "HEALTHY — BTSP enforcement active"
toadstool = "HEALTHY — riboCipher enforcement (sovereign compute router)"
barracuda = "HEALTHY — 90+ RPC methods, 24 capability domains, sub-ms IPC"
coralreef = "HEALTHY — shader dispatch core"
nestgate = "HEALTHY — BTSP authentication required (CAS authority)"
rhizocrypt = "HEALTHY — alive, uptime 77,129s, provenance authority"
loamspine = "HEALTHY — method routing active"
sweetgrass = "HEALTHY — riboCipher enforcement (data braid)"
biomeos = "HEALTHY — v4.55.0, API on :9800 + UDS, 7 modes available"
squirrel = "HEALTHY — alive v0.1.0, agent orchestration"
petaltongue = "HEALTHY — alive, rendering coordinator"

[gpu]
status = "ACTIVE — arXiv data produced, HMC benchmark complete"
device = "NVIDIA GeForce RTX 4060 (DiscreteGpu, SM89 Ada Lovelace)"
backend = "Vulkan 1.4.312"
shader_f64 = "YES — 14/9 builtins native"
max_buffer_size_mb = 1024
max_workgroup_x = 256
bcs_throughput = "2.1M nuclei/sec (batch 8192)"
eigensolve_throughput = "14,941 matrices/sec (512×30dim)"
l2_hfb_pipeline = "COMPLETE — 18 nuclei/eval, 434s/eval, GPU-resident"
hmc_8x4 = "39.6 ms/traj (32.3× speedup vs CPU)"
hmc_16x4 = "551.3 ms/traj (37.5× speedup vs CPU)"
hmc_32x8 = "2199.5 ms/traj (37.5× speedup vs CPU)"
hmc_strategy = "DF64 on FP32 cores — Concurrent (Omelyan, n_md=10, β=6.0)"

[tower_atomic_benchmark]
mode = "loopback (127.0.0.1:7700)"
latency_avg_ms = 0.134
latency_p50_ms = 0.131
latency_p95_ms = 0.186
setup_avg_ms = 0.074
throughput_mbps = 17242

[validation]
gate_deployment = "PROVEN — 22/22 PASS (Wave 155n)"
external_deploy = "PROVEN — depot.primals.eco over public internet, no WireGuard"
own_genetic_lineage = "PROVEN — family 89df7a2d, bearDog entropy-seeded"
btsp_trust_boundary = "PROVEN — 29,294 foreign peer rejections"
nucleus_boot = "PROVEN — 13/13, 32 sockets, 86MB RSS, 21h stable"
gpu_compute = "PROVEN — RTX 4060 via gnu barracuda, BCS + eigensolve validated"
ipc_round_trip = "PROVEN — sub-ms JSON-RPC to all 13 primals"
j18_portability = "PROVEN — user-space paths, XDG runtime, no system dirs"
g17_reconstitution = "PROVEN — public depot + own lineage + no mesh"

[guidestone]
status = "59/59 ALL CHECKS PASSED"
papers = "43 (Wilson Gradient Flow), 44 (BGK Dielectric), 45 (Kinetic-Fluid Coupling)"
runtime_seconds = 546
precision_highlight = "gradient_flow_unitarity = 2.23e-13 (tol: 1e-10)"

[arxiv_contribution]
section = "3.4 Multi-Vendor Benchmarks"
paper = "SU(2) Lattice QCD on Consumer GPU"
data_ready = true
gpu_row = "RTX 4060 | SM89 Ada Lovelace | 8 GB | 39.6 ms (8⁴) | 551.3 ms (16⁴)"

[springs_readiness]
hotspring = "ACTIVE — arXiv data produced, full HMC scaling, guideStone 59/59"
footprint = "READY — NUCLEUS operational, nestGate + petalTongue alive"
science_pipeline = "READY — 128GB RAM, 479GB free storage, CAS primals live"

[observations]
dual_binary_pattern = "musl static (NUCLEUS IPC) + gnu dynamic (GPU Vulkan) — same depot, different linkage"
gpu_generation_proof = "Ada Lovelace (SM89) validates alongside Ampere (SM86) — same WGSL shaders, same physics"
arxiv_status = "RTX 4060 row ready for hotSpring team integration into whitePaper/subGen/"
next_steps = "footPrint GIS, hotSpring QCD production, Aug 2 interruption readiness"
