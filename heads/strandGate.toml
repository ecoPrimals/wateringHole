# Gate: strandGate | Updated: 2026-08-13T11:55:00Z
# strandGate is GPU/NPU/CPU science compute. hotSpring QCD + silicon experiments.
# This file is owned exclusively by this gate. No other gate writes here.

[meta]
gate = "strandGate"
updated = "2026-08-13T13:04:00Z"
wave = "157k"
role = "SCIENCE COMPUTE — GPU (RTX 3090 + RX 6950 XT), NPU (AKD1000), CPU (EPYC 7452 128T)"
posture = "Capability-first silicon mapping. All fixed-function units are exploration targets. Streaming encoder upstream (43%→85-95% GPU util). DF64 RDNA2 hang fixed. TMU PRNG wired. ROP assessed. RT/tessellation/NVENC mapped for QCD."

[nucleus]
services = 15
status = "ALL RUNNING"
biomeos = "active (rebuilt Aug 8 — dispatch fix 44c40191 + riboCipher auto-detect 1ff5859c)"
toadstool = "active (rebuilt Aug 8)"
songbird = "active v0.2.1 (rebuilt — gossip.inject seam live, 5 mesh peers)"
barracuda = "active (Dual-lane Neural API)"
swarmvine = "active (TCP :7800, epidemic sweep 30s, gossip engine responding)"

[silicon]
nvidia = "RTX 3090 (SM86, 24GB GDDR6X)"
amd = "RX 6950 XT (RDNA2/Navi21, 16GB GDDR6)"
npu = "BrainChip Akida AKD1000 (80 NPs, VFIO-bound)"
cpu = "AMD EPYC 7452 (32C/64T × 2 = 128 threads)"

[silicon_paths_activated]
shader_fp32 = "STREAMING — single encoder for N_md×8 passes, eliminates 320 submit+poll round-trips"
shader_fp64 = "NATIVE + CONCURRENT — strategy-aware routing (Native on 3090, DF64 on 6950 XT)"
tmu_prng = "WIRED — Box-Muller log/cos/sin via texture lookup, frees ALU during momentum gen"
rop_blend = "7.8 Gscatter/s (RTX 3090) — force accumulation (assessed, compute atomic default)"
rasterizer = "63 Msites/s — spatial binning"
depth_buffer = "16 Mpx/s — distance field"
subgroup = "12.3 Gelements/s — warp shuffle reduction"
tmu_interpolation = "15.4 Gsamples/s — multigrid prolongation"
indirect_dispatch = "21µs/cycle — GPU self-scheduling"
timestamp = "1ns resolution — hardware profiling"
video_encoder = "NVENC/VAAPI detected (ffmpeg integration pending)"

[hotspring]
head = "ac98c0d"
thermalization = "75/87 configs cached (SU3 32^4 DONE, SU4 24^4 in progress)"
milc_delta = "3e-9 (machine epsilon validated)"
lanczos = "9.6x GPU speedup, eigenvalues match at 2e-15"
production_modules = "render_force_accum, tmu_multigrid, adaptive_dispatch, streaming_md_encoder"

[silicon_saturation_aar]
date = "2026-08-13"
commit = "2ff33c10"
summary = "Full Silicon Saturation — Compute Trio Evolution (barraCuda 2ff33c10)"
phase1_streaming = "GpuHmcTrajectory::run_streaming() — single encoder for all MD. Expected: 43%→85-95% GPU util, 140s→60-80s per trajectory on RTX 3090."
phase2_tmu = "GpuHmcLeapfrog::with_tmu() — TMU texture lookup for Box-Muller PRNG. Lights up TMU units during momentum gen."
phase3_df64_fix = "DF64 WG128→WG64 + 2D dispatch split. Root cause: VGPR exhaustion on RDNA2 (su3_link_update_df64 needs ~200+ regs/thread, WG128=4 wave32s saturates 1024 VGPR file). AMD 6950 XT can now run Concurrent strategy at 32^4."
phase4_rop = "Render-pass ROP blend validated (hotSpring prototype). 7.8G scatter-adds/s. Compute atomic remains default; render path for multi-pole RHMC."
units_lit = "ShaderCore (FP32 streaming), FP64 (native/concurrent), TMU (PRNG), ROP (assessed), Subgroup (reductions)"

[silicon_exploration]
posture = "All units are exploration targets. Capability precedes use case. Every fixed-function block solves a physics problem at wire speed."
rt_cores = "MAPPED — spatial query accelerator (BVH O(log n)). QCD: Wilson loop tracing, parameter-space nearest-neighbor, deformed lattice queries, multigrid coarsening. Benchmarked: 1.5 Mtri/s (3090), 0.1 Mtri/s (6950 XT). Not deployed: regular 32^4 lattice uses O(1) index arithmetic for neighbors."
tessellation = "THEORIZED — hardware h-refinement engine. QCD: adaptive multigrid coarsening/refinement, non-uniform lattice generation, domain-adapted stencils near boundaries/defects. Zero QCD benchmarks. Exploration target for multi-scale campaigns."
rasterizer = "MEASURED — coverage/binning engine. QCD: domain decomposition (site→cell assignment), Voronoi coarsening via depth buffer. 63 Msites/s (3090). Partially wired via ROP force path."
video_encoder = "MEASURED — temporal coherence compressor (NVENC/VAAPI). QCD: config archival 61:1 ratio, trajectory streaming, checkpoint compression. Zero ALU contention (separate ASIC). Integration pending (ffmpeg path)."
depth_buffer = "MEASURED — nearest-site lookup engine. QCD: Voronoi coarsening, multigrid prolongation weights, smearing radius queries. 16 Mpx/s."

[ssh_discipline]
github_remotes = "ZERO"
access_model = "Forgejo SSH key 'strandGate'. golgiBody depot via rsync."

[depot]
binary_date = "2026-08-08 (rebuilt depot — biomeOS, songBird, swarmVine, toadStool, skunkBat)"
source = "rsync from golgi:/srv/depot/primals/x86_64-unknown-linux-musl/"
swarmvine = "deployed as service (TCP :7800, epidemic sweep, systemd enrolled)"
