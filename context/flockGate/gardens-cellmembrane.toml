[braid]
gate = "flockGate"
project = "gardens/cellMembrane"
updated = "2026-05-31T10:03:56-04:00"
updated_by = "flockGate"
ttl_hours = 48
wave = 63

[strands.focus]
summary = "K-NOME hook live — context.sense auto-injects on sessionStart, rule fallback in place"
status = "active"

[strands.breadcrumbs]
trail = [
    ".cursor/hooks.json",
    ".cursor/hooks/context-sense.sh",
    ".cursor/rules/context-braid-workflow.md",
    "src/context.rs",
]

[strands.next]
actions = [
    "Implement template system for recurring K-NOME patterns",
    "Archive old acked impulses",
    "Test hook in fresh Cursor session",
]

[strands.notes]
body = "Hook injects as additional_context on sessionStart. Merges all gates' braids + pending impulses. Rule file covers cases where hook isn't supported. Template system next: infra/wateringHole/context/templates/{name}.toml"
