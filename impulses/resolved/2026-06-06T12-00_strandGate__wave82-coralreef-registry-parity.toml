# RESOLVED: coralReef capability_registry.toml — ecosystem parity item
#
# config/capability_registry.toml created and pushed. All P1 parity
# items for coralReef are complete.

[impulse]
id = "wave82-coralreef-registry-parity"
type = "resolution"
from = "strandGate"
to = "eastGate"
date = "2026-06-06"
priority = "P1"
status = "resolved"

[resolution]
commit = "d11cb58a"
branch = "main"
summary = """
Created config/capability_registry.toml per Wave 78 parity standard.
Declares all 16 served methods across 6 domains (shader, health,
identity, capability, btsp, auth), 3 consumed capabilities, GPU targets
(NVIDIA SM35-SM120, AMD GCN5-RDNA4), and per-operation cost/stability
metadata for biomeOS dispatch graph.

coralReef has ZERO remaining P0/P1 items:
- Headless fix: RESOLVED (0881ca28) — default-members excludes amd-isa-gen
- capability_registry.toml: RESOLVED (d11cb58a) — config/ directory
- VPS-ready: binary starts clean without ./specs/ or GPU hardware
- 3307 tests, zero clippy warnings, zero unsafe in production
"""

[resolution.file]
path = "config/capability_registry.toml"
format = "toml"
domains = ["shader", "health", "identity", "capability", "btsp", "auth"]
methods_count = 16
consumed = ["compute.dispatch", "capability.register", "ipc.heartbeat"]
