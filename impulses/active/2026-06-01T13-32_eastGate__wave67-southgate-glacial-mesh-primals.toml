[impulse]
id = "2026-06-01T13-32-eastGate-wave67-southgate-glacial-mesh-primals"
type = "frago"
priority = "high"
wave = 67

[from]
gate = "eastGate"

[to]
gates = ["southGate"]

[content]
subject = "Wave 67 glacial cutover — Songbird socket fix + biomeOS capability.call + bearDog S4 — pull wateringHole, plan in GLACIAL_CUTOVER_PLAN.md"
body = """
southGate owns the three primals on the glacial critical path:

1. SONGBIRD SECURITY SOCKET FIX (P0 BLOCKER)
   songbird_http_client must honor --security-socket / BEARDOG_SOCKET
   instead of hardcoded /tmp/neural-api-*.sock. Blocks federation TLS
   and cross-gate capability.call routing across all gates.

2. BIOMEOS capability.call RPC (P0 BLOCKER)
   Implement JSON-RPC method (currently returns -32601). Required for
   cross-gate capability routing — glacial criterion #2.

3. BEARDOG S4 AUTH CONFIG (P0)
   Ensure bearDog BTSP auth services are configured so ironGate can
   begin formal 7-day shadow validation. S4 is the last sovereignty
   shadow blocking stadial entry.

4. CROSS-GATE MESH PARTNER (Phase 1)
   After fixes 1-2, eastGate will run discovery.peers smoke test
   (eastGate <-> southGate) and s_covalent_mesh live validation.
   Confirm SONGBIRD_PEERS + federation after Songbird fix.

Gate ownership confirmed: Songbird, biomeOS, bearDog are southGate primals.
See GATE_TEAM_COORDINATION_MATRIX.md for full ecosystem mapping.

Full plan: GLACIAL_CUTOVER_PLAN.md
Pull wateringHole for all updates.
"""

[meta]
created = "2026-06-01T13:32:43-04:00"
ack_required = true
