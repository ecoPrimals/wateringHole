# FRAGO: Cross-Topology Validation & Autonomous Gate Operations
# Replaces: wave104-cross-deployment-readiness (archived — all P1s resolved, 12/16 items done)
# Purpose: validate across ALL arch, WAN, LAN, and topologies as gates redeploy

[impulse]
id = "wave106-cross-topology-validation"
type = "frago"
from = "eastGate"
to = ["cellMembrane", "songBird", "biomeOS", "primalSpring", "all-gates"]
date = "2026-06-09"
priority = "P2"
owner = "eastGate + cellMembrane"
note = """
The deployment surface works: VPS builds, all gates fetch, 13/13 NUCLEUS alive on
eastGate + VPS. Mesh is LIVE (eastGate↔golgiBody). Transport 11/11. Depot 14/14 x86_64
+ 14/14 aarch64 built. Zero P1 blockers.

What remains is CROSS-TOPOLOGY VALIDATION: proving the deployment model works on every
gate, every arch, every network topology. Each gate that comes online is a validation
opportunity. We evolve the tooling as we go — each redeployment should be easier than
the last.

Companion: AAR_WAVE105_COMPREHENSIVE_CROSS_DEPLOYMENT_JUN09_2026.md (lessons learned)
"""

# ──────────────────────────────────────────────────────────────
# CURRENT STATE (Wave 106, Jun 10 2026 01:00 UTC)
# ──────────────────────────────────────────────────────────────

[current_state]
p1_blockers = "ZERO. Ecosystem in pure validation mode — push to stadial."
depot_authority = "VPS (peptidoglycan). Post-primordial standard ENFORCED."
mesh = """LIVE — 3-gate collective: eastGate ↔ golgiBody(VPS) ↔ ironGate.
eastGate federation port 7700 LISTENING (bound *, accepting LAN + WAN).
ironGate: VALIDATED as 3rd mesh node (mesh.init to VPS relay, 1 reachable peer, direct path).
strandGate: VALIDATED LAN re-enrollment (Wave 106 ACK — mesh.init to eastGate + VPS, 2 peers, quality 1.0).
songBird: mesh persistence SHIPPED (peers.toml + auto-reconnect). Federation port bind fix SHIPPED.
cellMembrane: gate.bootstrap + gate.status SHIPPED. --dry-run SHIPPED. VPS songbird + biomeOS depot refreshed."""
transport = "11/11 non-exempt COMPLETE."
depot_x86 = "13/13 BLAKE3 VERIFIED (VPS authority). songbird + biomeOS rebuilt on VPS (Wave 107)."
depot_aarch64 = "14/14 BUILT. 10/13 RUNNING on grapheneGate (biomeOS v4.18 TCP-only adoption). 3 primals pending upstream bind_transport()."
wan_depot = "13/13 serving HTTP 200 from membrane.primals.eco/depot/. checksums.toml endpoint provisioning READY."
sovereignty = "S1-S3 GRADUATED. S4 ending."
strandgate = "Wave 106 ACK: LAN x86_64 re-enrollment PROVEN. Bin target debt RESOLVED (22 alias conflicts fixed, 1089 tests, 0 failures)."
irongate = "Wave 106: 3rd mesh node VALIDATED. gate.bootstrap + gate.status operational."
deterministic_deployment = """CODIFIED + EVOLVED (Wave 107):
- Source divergence FIX: plasmid.refresh now reads from depot (not stale /tmp staging)
- Checksum coherence: refresh warns on DIVERGENCE if binary != checksums.toml
- gate.status: local health probe (depot integrity, mesh, primals, freshness)
- gate.bootstrap --dry-run: preview without side effects
- WAN checksums: gates can verify via HTTPS without git
- Atomic publish: harvest auto-commits + pushes checksums.toml + provenance.toml"""

# ──────────────────────────────────────────────────────────────
# VALIDATION MATRIX — what needs to be proven on each topology
# ──────────────────────────────────────────────────────────────

[validation.lan_x86_64]
description = "LAN gates (eastGate, strandGate, ironGate, southGate) — x86_64-musl over UDS"
proven_on = ["eastGate (13/13 alive, VPS-fetched)", "golgiBody VPS (13/13 alive)", "strandGate (Wave 106 ACK — LAN re-enrollment PROVEN, mesh rejoin after extended offline)", "ironGate (3rd mesh node VALIDATED — VPS relay, 13 fetched, 12/13 started)"]
needs_validation = ["southGate (cross-subnet — needs power-on)"]
gate_playbook = """When gate comes online:
  1. ssh <gate> 'curl -O https://membrane.primals.eco/depot/x86_64-unknown-linux-musl/{primal}' × 13
  2. chmod +x all binaries
  3. Start songbird with federation port, mesh.init to VPS + known LAN peers
  4. Start remaining NUCLEUS from depot
  5. Verify health sweep — 13/13 JSON-RPC alive"""

[validation.wan_x86_64]
description = "WAN gates (flockGate) — x86_64-musl, depot via HTTPS, mesh via VPS relay"
proven_on = ["flockGate (4/5 e2e PASS — fetch/launch/mesh.init/health OK)"]
needs_validation = ["flockGate mesh peering (VPS songbird relay now LIVE — should unblock)"]
gate_playbook = """When flockGate is online:
  1. plasmid.fetch --source wan (or curl from membrane.primals.eco/depot/)
  2. mesh.init to 157.230.3.183:7700 (golgiBody VPS relay)
  3. Verify discovery.peers shows transitive discovery of eastGate
  4. Full NUCLEUS health sweep"""

[validation.aarch64_musl]
description = "ARM gates (grapheneGate/Pixel 8) — aarch64-musl via ADB"
proven_on = ["grapheneGate (9/13 running — skunkbat, toadstool, barracuda added via SELinux --no-uds/--no-unix adaptation)"]
needs_validation = ["3 remaining: coralreef, nestgate, petaltongue — exit fatally on UDS bind SELinux denial, need TCP-only graceful degradation. biomeOS TCP-only fallback SHIPPED (v4.18, b8ddf351)."]
gate_playbook = """Remaining work (primalSpring parallel team):
  1. ✅ BIOMEOS_SOCKET_DIR env var shipped
  2. ✅ deploy_pixel.sh SELinux adaptation for 9/13 primals
  3. ✅ TCP-only fallback infrastructure shipped (ipc::server_bind + PRIMAL_BIND_MODE=fallback)
  4. Upstream adoption: coralreef, nestgate, biomeOS, petaltongue adopt bind_transport()
  5. Redeploy via deploy_pixel.sh, verify 13/13 alive"""

[validation.vps_nucleus]
description = "VPS NUCLEUS (golgiBody inner membrane) — x86_64 depot authority"
proven_on = ["golgiBody (13/13 running, songbird:7700 mesh LIVE)"]
needs_validation = ["Automated refresh when peptidoglycan builds new binaries"]

# ──────────────────────────────────────────────────────────────
# REMAINING WORK — focused, deduplicated, team-routed
# ──────────────────────────────────────────────────────────────

# --- P1: Gate Self-Healing (blocks autonomous operation) ---

[resolved.nucleus_supervision]
id = "NUCLEUS-SUPERVISION"
owner = "biomeOS"
priority = "DONE"
description = "SHIPPED (biomeOS v4.17, commit a4a59245). LifecycleManager auto-restarts crashed primals."
resolution = """Option B implemented — lifecycle watchdog via LifecycleManager:
1. ✅ ManagedPrimal extended with binary_path + node_id for launch context
2. ✅ register_primal_binary() — NUCLEUS registers primals with depot binary path
3. ✅ respawn_primal_binary() — resurrection path: kill old → clean socket → respawn from binary → PID file → wait for socket → Incubating state
4. ✅ attempt_resurrection() falls back to binary_path when deployment_node is None
5. ✅ Exponential backoff (2s base, 60s max), max 5 attempts before Dead state
6. Health poll (10s) → degrade → backoff → kill → respawn → incubate → health poll → active"""

[remaining.mesh_persistence]
id = "MESH-PERSISTENCE"
owner = "songBird"
priority = "DONE"
description = "SHIPPED (Wave 106, commit 1df7ef90). Peers persist to ~/.local/share/songbird/peers.toml"
why = """RESOLVED. mesh.init now appends peers to persistent store (merge-dedup by node_id).
On startup, spawn_mesh_seed loads persisted peers as autonomous fallback when SONGBIRD_PEERS
is unset. Gates auto-reconnect on reboot without operator intervention."""
action = """SHIPPED:
1. ✅ Persist peers to ~/.local/share/songbird/peers.toml (SONGBIRD_DATA_DIR override available)
2. ✅ On startup, auto-connect to persisted peers (spawn_mesh_seed fallback)
3. ✅ mesh.init appends to persistent store (spawn_blocking, merge-dedup)
4. Future: mDNS/broadcast for LAN peer auto-discovery (already wired in discovery layer)"""

[resolved.cascade_auto_fetch]
id = "CASCADE-AUTO-FETCH"
owner = "cellMembrane"
priority = "DONE"
description = "SHIPPED (Wave 106, commit b6c9fa0). Post-cascade auto-fetch hook."
resolution = """When plasmidBin is pulled during cascade and checksums.toml changes,
automatically triggers WAN fetch to update local binaries. Closes the loop from
peptidoglycan-build to gate-running."""

# --- P2: Cross-Topology Validation (blocks stadial) ---

[remaining.eastgate_federation_port]
id = "EASTGATE-FEDERATION-PORT"
owner = "eastGate ops"
priority = "DONE"
description = "SHIPPED (Wave 106, commit 1df7ef90). Federation port now binds 0.0.0.0 when set."
why = """RESOLVED. Root cause identified and fixed: when SONGBIRD_FEDERATION_PORT was set,
the HTTP/federation server still defaulted bind to 127.0.0.1 (localhost). Remote peers
got connection refused. Fix: setting SONGBIRD_FEDERATION_PORT (env or --federation-port CLI)
now auto-promotes bind to 0.0.0.0 (all interfaces). SONGBIRD_FEDERATION_BIND still overrides."""
action = """SHIPPED. To use on any gate:
  SONGBIRD_FEDERATION_PORT=7700 songbird server --socket /run/user/$(id -u)/biomeos/songbird.sock
Port 7700 will now be visible in 'ss -tlnp' bound to 0.0.0.0 and accept incoming mesh connections."""

[resolved.irongate_mesh]
id = "IRONGATE-MESH"
owner = "ironGate ops"
priority = "DONE"
description = "RESOLVED (Wave 106, 2026-06-10 01:30 UTC). ironGate is 3rd mesh node."
resolution = """ironGate validated as 3rd mesh node in collective:
1. VPS:7700 confirmed reachable from ironGate (nc -z, 69ms)
2. songbird started with federation port 7700 + UDS socket
3. mesh.init to 157.230.3.183:7700 → {bootstrap_peers_added:1, initialized:true, node_id:ironGate}
4. mesh.status → {reachable_peers:1, paths:{direct:1}, relay_enabled:true}
5. 13 primals fetched via WAN, 12/13 started, health sweep confirmed alive

Mesh topology now: eastGate ↔ golgiBody(VPS) ↔ ironGate (3-gate collective)"""

[remaining.flockgate_wan_e2e]
id = "FLOCKGATE-WAN-E2E"
owner = "flockGate ops"
priority = "P2"
description = "Complete flockGate WAN e2e — VPS songbird relay is now LIVE"
why = """flockGate passed 4/5 WAN e2e stages. Was blocked on VPS songbird relay.
golgiBody songbird:7700 is now LIVE and meshed with eastGate. flockGate should be
able to mesh.init to VPS and reach the collective."""
action = """When flockGate is online:
1. mesh.init to 157.230.3.183:7700
2. Verify discovery.peers shows eastGate transitively
3. If PASS → 5/5 WAN e2e → stadial criterion 4 VALIDATED"""

[remaining.graphenegate_uds]
id = "GRAPHENEGATE-UDS"
owner = "primalSpring (parallel team) + upstream primal teams"
priority = "P2"
description = "3 primals need TCP-only graceful degradation for grapheneGate (10/13 with biomeOS fix)"
why = """coralreef, nestgate, petaltongue exit fatally on UDS bind SELinux denial.
biomeOS TCP-only fallback SHIPPED (v4.18, b8ddf351) — Neural API, API server, and NUCLEUS
spawning all gracefully degrade to TCP when UDS bind fails. --tcp-only flag unblocked in
release builds. biomeOS is now 10th primal running on grapheneGate.
Remaining 3 need upstream code changes to gracefully degrade to TCP-only when UDS is denied."""
action = """1. ✅ BIOMEOS_SOCKET_DIR + XDG_RUNTIME_DIR + per-primal overrides SHIPPED
2. ✅ 9/13 running via deploy_pixel.sh SELinux adaptation
3. ✅ biomeOS TCP-only fallback SHIPPED (v4.18) — 10/13
4. ✅ TCP-only fallback infrastructure SHIPPED (primalSpring ipc::server_bind module)
   - bind_transport(slug, BindMode::from_env()) — canonical server-side bind
   - PRIMAL_BIND_MODE env var (uds_only/tcp_only/fallback)
   - deploy_pixel.sh exports PRIMAL_BIND_MODE=fallback
   - primalSpring own server binary uses bind_transport as reference impl
   - s_tcp_fallback validation scenario (53rd, Transport track)
5. Upstream adoption: coralreef, nestgate, petaltongue adopt bind_transport()
5. Target: 13/13 alive on Pixel 8"""

# --- P2: Tooling (blocks automation) ---

[resolved.gate_bootstrap]
id = "GATE-BOOTSTRAP"
owner = "cellMembrane"
priority = "DONE"
description = "SHIPPED (Wave 106, commit b6c9fa0). membrane gate.bootstrap <gate-name>"
resolution = """One-command gate enrollment: detect arch → fetch depot → verify checksums
→ configure mesh → start NUCLEUS → health sweep. strandGate used it for LAN re-enrollment."""

[resolved.plasmid_fetch_fix]
id = "PLASMID-FETCH-FIX"
owner = "cellMembrane"
priority = "DONE"
description = "SHIPPED (Wave 106, commit b6c9fa0). VPS path normalization fixed."
resolution = "Remote path was {primal}-{arch} instead of {arch}/{primal}. Fixed."

[resolved.wave107_deployment_hardening]
id = "WAVE107-DEPLOYMENT-HARDENING"
owner = "cellMembrane"
priority = "DONE"
description = "SHIPPED (Wave 107, 2026-06-10). Post-stadial deployment hardening."
resolution = """6 fixes/features in 2 commits:
1. Source divergence fix — refresh reads from depot not stale /tmp staging
2. Checksum coherence — pre-push DIVERGENCE warning if binary != checksums.toml
3. gate.status — local health probe (depot integrity, mesh reachability, primals, freshness)
4. gate.bootstrap --dry-run — preview 6 phases without executing side effects
5. WAN checksums — caddy.depot.checksums route + fetch auto-downloads for zero-git verify
6. Atomic publish — harvest auto-commits/pushes checksums.toml + provenance.toml
Plus: gate.rs (936L) refactored into gate/mod.rs + gate/local.rs (both under 800L)"""

[remaining.sourdough_segfault]
id = "SOURDOUGH-SEGFAULT"
owner = "sourDough"
priority = "LOW"
description = "sourdough validate depot crashes (segfault)"
why = "Depot validation falls back to manual b3sum. Erodes trust in tooling."
action = "Run under RUST_BACKTRACE=1, investigate and fix."

# ──────────────────────────────────────────────────────────────
# GATE ENROLLMENT PLAYBOOK (for any gate that comes online)
# ──────────────────────────────────────────────────────────────

[playbook.gate_enrollment]
description = """Universal playbook for when any gate powers on and needs deployment.
Until membrane gate.bootstrap exists, this is the manual procedure."""

steps = """
# 1. Identify gate arch
ARCH=$(uname -m)  # x86_64 → x86_64-unknown-linux-musl, aarch64 → aarch64-unknown-linux-musl

# 2. Create depot directory
mkdir -p ~/Development/ecoPrimals/infra/plasmidBin/primals/${ARCH}-unknown-linux-musl/

# 3. Fetch all primals from VPS
for p in beardog songbird biomeos nestgate coralreef sweetgrass squirrel loamspine rhizocrypt skunkbat petaltongue barracuda toadstool; do
  curl -o primals/${ARCH}-unknown-linux-musl/$p https://membrane.primals.eco/depot/${ARCH}-unknown-linux-musl/$p
  chmod +x primals/${ARCH}-unknown-linux-musl/$p
done

# 4. Verify checksums (compare against checksums.toml from git)
for p in primals/${ARCH}-unknown-linux-musl/*; do
  b3sum $p
done

# 5. Start songbird with federation
SONGBIRD_FEDERATION_PORT=7700 SONGBIRD_PRODUCTION_BIND_ADDRESS=0.0.0.0 \
  primals/${ARCH}-unknown-linux-musl/songbird server --socket /run/user/$(id -u)/biomeos/songbird.sock &

# 6. Initialize mesh (to VPS relay + any known LAN peers)
echo '{"jsonrpc":"2.0","method":"mesh.init","params":{"node_id":"<GATE_NAME>","peers":["157.230.3.183:7700"]},"id":1}' | \
  socat - UNIX-CONNECT:/run/user/$(id -u)/biomeos/songbird.sock

# 7. Start remaining NUCLEUS
for p in beardog biomeos nestgate coralreef sweetgrass squirrel loamspine rhizocrypt skunkbat petaltongue barracuda toadstool; do
  primals/${ARCH}-unknown-linux-musl/$p server --socket /run/user/$(id -u)/biomeos/${p}.sock &
done

# 8. Verify health
for sock in /run/user/$(id -u)/biomeos/*.sock; do
  echo '{"jsonrpc":"2.0","method":"health.liveness","id":1}' | timeout 2 socat - UNIX-CONNECT:$sock
done
"""

# ──────────────────────────────────────────────────────────────
# OUTCOME
# ──────────────────────────────────────────────────────────────

[outcome]
expected = """
WAVE 107 STATE (post-stadial tooling evolution):

WAVE 107 — cellMembrane deployment hardening:
  ✅ Source divergence FIX → plasmid.refresh defaults to depot (not /tmp/primalspring-deploy)
  ✅ Checksum coherence → refresh emits DIVERGENCE warning on hash mismatch pre-push
  ✅ gate.status → local gate health probe (no SSH — depot, mesh, primals, freshness)
  ✅ gate.bootstrap --dry-run → preview all 6 phases without side effects
  ✅ caddy.depot.checksums → WAN endpoint for checksums.toml (zero-git verification)
  ✅ WAN checksum fetch → plasmid.fetch --source wan auto-downloads checksums for verification
  ✅ Atomic publish → harvest auto-commits + pushes checksums.toml + provenance.toml
  ✅ gate.rs refactored → gate/mod.rs (298L) + gate/local.rs (620L), well under 800L limit
  ✅ VPS depot refreshed → songbird (23MB, mesh persistence) + biomeOS (21MB, v4.17/v4.18)

WAVE 106 (all resolved):
  ✅ strandGate → LAN x86_64 re-enrollment PROVEN
  ✅ ironGate → 3rd mesh node VALIDATED
  ✅ gate.bootstrap → SHIPPED + VALIDATED on strandGate + ironGate
  ✅ plasmid.fetch VPS path → FIXED
  ✅ cascade auto-fetch → SHIPPED
  ✅ mesh persistence + federation port fix → SHIPPED by songBird
  ✅ NUCLEUS supervision → SHIPPED (biomeOS v4.17)
  ✅ TCP-only fallback → SHIPPED (biomeOS v4.18 + primalSpring ipc::server_bind)
  ✅ Deterministic deployment standard → CODIFIED (6 invariants)

WAVE 107 — primalSpring TCP-only adoption:
  ✅ grapheneGate 13/13 TCP fallback → ALL ADOPTED (coralReef, nestGate, petalTongue)

REMAINING (1 P2 — zero P1):
  1. flockGate → WAN e2e 5/5 (VPS songbird LIVE, awaiting gate power-on)
  2. grapheneGate → aarch64 rebuild + Pixel 8 redeploy to validate 13/13 alive
  3. southGate → mesh enrollment in progress (depot refreshed, cascade operational)

DEPLOYMENT IS DETERMINISTIC, AUTONOMOUS, AND OBSERVABLE:
gate.bootstrap + gate.status + cascade auto-fetch + NUCLEUS supervision + atomic publish
= zero manual steps, self-healing gates, divergence detection pre-push.

Companion: AAR_CELLMEMBRANE_WAVE106_DETERMINISTIC_DEPLOYMENT_JUN10_2026.md
"""
