# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 138
sub = "a"
date = "2026-07-13"
posture = "PUBLIC + SOVEREIGN. Wave 137b CLOSED — deep review complete, all discussion resolved, 7 docs updated, 4 handoffs/impulses fossilized. Domain identity formalized (primals.eco/primal.eco/nestgate.io). Wave 138: entropy ceremonies, hardware sovereignty, multi-arch substrate. 6 carried items + 4 evolution targets."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "flockGate", "golgi", "grapheneGate"]
offline = ["strandGate"]
