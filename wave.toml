# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 155
sub = "g"
date = "2026-07-28"
posture = "GATE WORKLOAD DISTRIBUTION LIVE. westGate Tower Atomic LIVE (bearDog+songBird+skunkBat systemd, UDS+federation :7700). strandGate synced (42 repos, compute trio ready). westGate hardware corrected: AMD Ryzen 7 5700X/64GB/2TB NVMe/5x14TB raw HDD (not i7-4771). Blurb validated: HTTPS public pull unblocks fresh gates without SSH. Shallow roots pattern confirmed — GitHub clones need fresh Forgejo clone. 6/8 jelly strings (J8 key portal opened). Nest Atomic after Tower stable."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "flockGate", "golgi", "grapheneGate", "northGate", "westGate"]
enrolling = ["strandGate", "blueGate", "swiftGate", "southGate"]
offline = []
