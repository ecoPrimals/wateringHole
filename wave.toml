# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 155
sub = "c"
date = "2026-07-27"
posture = "neuralAPI DELIVERED. biomeOS semantic dispatch for Tower (8 signals), Node (3 signals), Nest (8 signals). 26 signal graphs, all platform_native transport. Nest Atomic STARTING — provenance trio foundation shipped, nest.verify + nest.federate graphs define cross-gate pipeline. 19 atomic-tier translations in capability_registry. Cross-platform socket templates (named pipes + TCP fallback). Jelly strings J1-J7 triaged. Next: live validation on gates."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "flockGate", "golgi", "grapheneGate", "northGate"]
enrolling = ["strandGate", "westGate", "blueGate", "swiftGate", "southGate"]
offline = []
