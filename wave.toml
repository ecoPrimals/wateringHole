# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 155
sub = "n"
date = "2026-07-31"
posture = "ZERO P0/P1/P2. gen4 COMPLETE. gen5 VALIDATED. biomeOS v4.56 (G22 IN PROGRESS). P2 platform detection FIXED (J12 UNBLOCKED). westGate ZFS 50.7 TB. southGate VALIDATION GATE. 14 AARs fossilized. 9/11 jelly strings killed. 23 glacial goals. Clearing deployment items → springs+gardens phase."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "golgi", "grapheneGate", "northGate", "westGate", "strandGate", "blueGate"]
degraded = ["flockGate"]
validation = ["southGate"]
enrolling = ["swiftGate"]
offline = []
