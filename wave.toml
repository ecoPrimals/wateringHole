# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 155
sub = "i"
date = "2026-07-29"
posture = "DEEP DEBT + VALIDATION WAVE. 8 primals shipped simultaneous debt sweeps. CAS on ZFS verified (3,119 objects). Provenance Trio 6/7 live. RTX 3090 GPU profiled (FP64 ~104T). sporeGate depot refreshed (19 binaries, health 5/11→9/11). nestGate 13K+ tests, toadStool S346 security hardening, cellMembrane fail-closed. P0 blocker: biomeOS BTSP composition broker. 27 signal graphs. ~55K+ tests."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "flockGate", "golgi", "grapheneGate", "northGate", "westGate", "strandGate"]
enrolling = ["blueGate", "swiftGate", "southGate"]
offline = []
