# SPDX-License-Identifier: CC-BY-SA-4.0
#
# wave.toml — Ecosystem wave coordination (single writer: overwatch)
#
# Authority: eastGate overwatch (sole writer — no other gate modifies this file)
# Consumed by: all gates, membrane temporal.cascade --check, s_ecosystem_freshness
#
# This file tracks the human-authored wave state. Per-gate HEAD SHAs live in
# heads/<gate>.toml (each gate writes only its own file — zero conflicts).

[wave]
id = 155
sub = "v"
date = "2026-08-04"
posture = "WAVE 155v/156d PROVENANCE DIVERGENCE RESOLVED (122x improvement, 0.3/s->37.6/s, canonical architecture). THREE-DOMAIN TOPOLOGY SPEC (primals.eco/nestgate.io/primal.eco k-derm). strandGate 12^4 PAPER-READY, plaquette normalization RESOLVED, Rung 1 UNBLOCKED. esotericWebb V30, footPrint 628 tests (Phase 2 deploy ready), petalTongue pushed (CAS refactor). Nanowire->Primal Builder Phase 2a DONE. sweetGrass trailer aligned (1,645 tests). Provenance loop CLOSED (bearDog sig in braid). ~135K tests, 13/13 GREEN."
publisher = "eastGate-overwatch"

[gates]
online = ["sporeGate", "eastGate", "ironGate", "golgi", "grapheneGate", "northGate", "westGate", "strandGate", "blueGate", "southGate", "biomeGate"]
degraded = ["flockGate"]
validation = []
enrolling = ["swiftGate", "steamGate", "reefGate"]
offline = []
